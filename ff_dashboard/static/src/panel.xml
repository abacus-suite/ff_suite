<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="ff_dashboard.Panel">
        <div class="o_action_manager ff_panel d-flex h-100">

            <!-- Sidebar -->
            <div class="ff_panel_side" t-att-class="state.collapsed ? 'ff_panel_side_narrow' : ''">
                <div class="ff_panel_brand">
                    <img src="/ff_base/static/description/icon.png" alt="Aixolo"/>
                    <span t-if="!state.collapsed">Aixolo</span>
                    <button class="btn btn-sm ms-auto ff_panel_collapse" t-on-click="toggleSidebar">
                        <i class="fa" t-att-class="state.collapsed ? 'fa-angle-right' : 'fa-angle-left'"/>
                    </button>
                </div>
                <t t-foreach="sections" t-as="item" t-key="item.key">
                    <div class="ff_panel_link"
                         t-att-class="state.section === item.key ? 'ff_panel_link_active' : ''"
                         t-on-click="() => this.openSection(item.key)">
                        <i class="fa" t-att-class="item.icon"/>
                        <span t-if="!state.collapsed" t-esc="item.label"/>
                    </div>
                </t>
                <div class="ff_panel_side_foot" t-if="!state.collapsed">
                    More sections are on the way.
                </div>
            </div>

            <!-- Content -->
            <div class="ff_panel_main flex-grow-1 overflow-auto" t-if="state.section === 'dashboard'">
                <t t-if="state.loading">
                    <div class="ff_skeleton mb-3" style="height: 72px;"/>
                    <div class="row g-3">
                        <div class="col-xl-3 col-md-6"><div class="ff_skeleton" style="height: 130px;"/></div>
                        <div class="col-xl-3 col-md-6"><div class="ff_skeleton" style="height: 130px;"/></div>
                        <div class="col-xl-3 col-md-6"><div class="ff_skeleton" style="height: 130px;"/></div>
                        <div class="col-xl-3 col-md-6"><div class="ff_skeleton" style="height: 130px;"/></div>
                        <div class="col-12"><div class="ff_skeleton" style="height: 300px;"/></div>
                    </div>
                </t>
                <t t-else="">
                    <!-- Greeting and period -->
                    <div class="ff_panel_head d-flex align-items-center gap-2 flex-wrap">
                        <div>
                            <h2 class="ff_hello mb-0"><t t-esc="greeting"/>, <span class="ff_wave">👋</span></h2>
                            <div class="ff_hello_sub">Here's what's happening today!</div>
                            <small class="text-muted">
                                <t t-esc="state.data.company"/> · Updated at <t t-esc="state.updatedAt"/>
                            </small>
                        </div>
                        <div class="ff_seg ms-auto">
                            <button class="ff_seg_btn" t-att-class="state.period === 'today' ? 'ff_seg_btn_active' : ''"
                                    t-on-click="() => this.setPeriod('today')">Today</button>
                            <button class="ff_seg_btn" t-att-class="state.period === 'week' ? 'ff_seg_btn_active' : ''"
                                    t-on-click="() => this.setPeriod('week')">This Week</button>
                            <button class="ff_seg_btn" t-att-class="state.period === 'month' ? 'ff_seg_btn_active' : ''"
                                    t-on-click="() => this.setPeriod('month')">This Month</button>
                        </div>
                        <button class="ff_icon_btn" title="Refresh" t-on-click="() => this.load()">
                            <i class="fa fa-refresh"/>
                        </button>
                        <button class="ff_btn_primary" t-on-click="() => this.openSection('live')">
                            <i class="fa fa-map-marker me-1"/>Live Map
                        </button>
                    </div>

                    <!-- Row 1: four headline figures -->
                    <div class="row g-3 mt-0">
                        <t t-foreach="kpiCards" t-as="kpi" t-key="kpi.key">
                            <div class="col-xl-3 col-md-6">
                                <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                     t-on-click="kpi.open">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                        <div class="ff_kpi_label" t-esc="kpi.label"/>
                                    </div>
                                    <div class="ff_kpi_value" t-esc="kpi.value"/>
                                    <div class="d-flex align-items-center gap-2 mt-1">
                                        <span class="ff_kpi_chip" t-esc="kpi.chip"/>
                                    </div>
                                    <div class="ff_kpi_note" t-esc="kpi.note"/>
                                    <i class="fa ff_kpi_ghost" t-att-class="kpi.icon"/>
                                </div>
                            </div>
                        </t>
                    </div>

                    <!-- Row 2: status gauge, teams, live map promo -->
                    <div class="row g-3 mt-0">
                        <div class="col-xl-4 col-lg-6">
                            <div class="ff_card h-100">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-user-circle-o"/></div>
                                    <div class="ff_card_title">Real Time Status</div>
                                    <span class="ff_live_pill ms-auto"><span class="ff_dot" style="background:#16A34A"/>Live</span>
                                </div>
                                <div class="d-flex align-items-center gap-3">
                                    <div class="ff_gauge flex-grow-1">
                                        <svg viewBox="0 0 180 110">
                                            <path d="M20 95 A70 70 0 0 1 160 95" fill="none" stroke="#FDE2E2" stroke-width="16"
                                                  stroke-linecap="round"/>
                                            <path d="M20 95 A70 70 0 0 1 160 95" fill="none" stroke="#16A34A" stroke-width="16"
                                                  stroke-linecap="round"
                                                  t-att-stroke-dasharray="gaugeArc.length"
                                                  t-att-stroke-dashoffset="gaugeArc.length - gaugeArc.filled"/>
                                            <text x="90" y="74" text-anchor="middle" class="ff_gauge_value"><t t-esc="gaugeArc.percent"/>%</text>
                                            <text x="90" y="92" text-anchor="middle" class="ff_gauge_ratio">
                                                <t t-esc="state.data.realtime.punched_in"/> / <t t-esc="state.data.realtime.total"/>
                                            </text>
                                        </svg>
                                        <div class="text-center small text-muted">Employees Punched In</div>
                                    </div>
                                    <div class="ff_legend">
                                        <div class="d-flex align-items-center gap-2">
                                            <span class="ff_dot" style="background:#16A34A"/>Punched In
                                            <b class="ms-auto ps-3" t-esc="state.data.realtime.punched_in"/>
                                        </div>
                                        <div class="d-flex align-items-center gap-2 mt-2">
                                            <span class="ff_dot" style="background:#DC2626"/>Punched Out
                                            <b class="ms-auto ps-3" t-esc="state.data.realtime.punched_out"/>
                                        </div>
                                        <div class="d-flex align-items-center gap-2 mt-2" t-if="state.data.realtime.inactive">
                                            <span class="ff_dot" style="background:#F59E0B"/>Idle
                                            <b class="ms-auto ps-3" t-esc="state.data.realtime.inactive"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-xl-4 col-lg-6">
                            <div class="ff_card h-100">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-sitemap"/></div>
                                    <div class="ff_card_title">Teamwise Attendance</div>
                                    <button class="btn btn-link btn-sm ms-auto p-0" t-on-click="() => this.openSection('attendance')">
                                        View All
                                    </button>
                                </div>
                                <div class="ff_scroll_220">
                                    <div class="ff_team_row" t-foreach="state.data.teams" t-as="team" t-key="team.id">
                                        <div class="d-flex align-items-center small">
                                            <span class="text-truncate" t-esc="team.name"/>
                                            <span class="ms-auto d-flex align-items-center gap-2">
                                                <span><span class="ff_dot" style="background:#16A34A"/><b t-esc="team.in"/></span>
                                                <span><span class="ff_dot" style="background:#DC2626"/><b t-esc="team.out"/></span>
                                                <span class="ff_team_pct"><t t-esc="team.pct or 0"/>%</span>
                                            </span>
                                        </div>
                                        <div class="ff_progress"><div t-attf-style="width: {{team.pct or 0}}%"/></div>
                                    </div>
                                    <div t-if="!state.data.teams.length" class="text-muted small py-2">No teams yet.</div>
                                </div>
                            </div>
                        </div>

                        <div class="col-xl-4">
                            <div class="ff_promo h-100">
                                <div class="ff_promo_text">
                                    <div class="ff_promo_title">Track Your<br/>Team in Real Time</div>
                                    <div class="ff_promo_sub">Field visibility<br/>for better decisions.</div>
                                    <button class="ff_promo_btn" t-on-click="() => this.openSection('live')">
                                        View Live Map <i class="fa fa-arrow-right ms-1"/>
                                    </button>
                                </div>
                                <div class="ff_promo_phone">
                                    <div class="ff_promo_screen">
                                        <i class="fa fa-map-marker"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Row 3: employees -->
                    <div class="row g-3 mt-0">
                        <div class="col-12">
                            <div class="ff_card">
                                <div class="d-flex align-items-center mb-2 gap-2 flex-wrap">
                                    <div class="ff_card_head mb-0">
                                        <div class="ff_card_icon"><i class="fa fa-id-badge"/></div>
                                        <div class="ff_card_title">Employees</div>
                                        <span class="ff_card_sub ms-2"><t t-esc="people.length"/> in your view</span>
                                    </div>
                                    <div class="ms-auto ff_search_box">
                                        <i class="fa fa-search"/>
                                        <input type="text" class="form-control form-control-sm"
                                               placeholder="Search employee, code or team..." t-on-input="onSearch"/>
                                    </div>
                                </div>
                                <div t-if="state.data.geocode_problem" class="ff_warn mb-2">
                                    <i class="fa fa-exclamation-triangle me-1"/>
                                    Addresses are unavailable, so coordinates are shown.
                                    Google said: <t t-esc="state.data.geocode_problem"/>
                                </div>
                                <div class="ff_scroll_320">
                                    <table class="table ff_table align-middle mb-0">
                                        <thead>
                                            <tr>
                                                <th style="width: 40px;">#</th>
                                                <th>Employee</th>
                                                <th>Team</th>
                                                <th>Status</th>
                                                <th>Last Location</th>
                                                <th>Battery</th>
                                                <th>Last Seen</th>
                                                <th class="text-center">Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <tr t-foreach="people" t-as="person" t-key="person.id"
                                                class="ff_row" t-on-click="() => this.openEmployee(person)">
                                                <td class="text-muted" t-esc="person_index + 1"/>
                                                <td>
                                                    <div class="d-flex align-items-center gap-2">
                                                        <img class="ff_avatar ff_avatar_round" t-att-src="person.avatar" alt=""/>
                                                        <div class="min-width-0">
                                                            <div class="fw-bold text-truncate" t-esc="person.name"/>
                                                            <div class="small text-muted" t-if="person.code">(<t t-esc="person.code"/>)</div>
                                                        </div>
                                                    </div>
                                                </td>
                                                <td class="text-muted small" t-esc="person.team or '—'"/>
                                                <td>
                                                    <span t-if="person.punched_in" class="ff_state_pill ff_state_in">Punched In</span>
                                                    <span t-else="" class="ff_state_pill ff_state_out">Punched Out</span>
                                                    <div class="small text-muted" t-if="person.at_client">
                                                        At <t t-esc="person.at_client"/>
                                                    </div>
                                                </td>
                                                <td class="small ff_address">
                                                    <t t-if="person.address"><t t-esc="person.address"/></t>
                                                    <t t-elif="person.lat or person.lng">
                                                        <t t-esc="person.lat.toFixed(5)"/>, <t t-esc="person.lng.toFixed(5)"/>
                                                    </t>
                                                    <span t-else="" class="text-muted">No location yet</span>
                                                </td>
                                                <td class="small">
                                                    <t t-if="person.battery">
                                                        <span class="ff_battery" t-att-class="person.battery &lt; 20 ? 'ff_battery_low' : ''">
                                                            <span t-attf-style="width: {{person.battery}}%"/>
                                                        </span>
                                                        <span class="text-muted ms-1"><t t-esc="person.battery"/>%</span>
                                                    </t>
                                                    <span t-else="" class="text-muted">—</span>
                                                </td>
                                                <td class="small text-muted">
                                                    <t t-if="person.last_ping_at" t-esc="ago(person.last_ping_at)"/>
                                                    <t t-elif="person.punched_at" t-esc="clock(person.punched_at)"/>
                                                    <t t-else="">—</t>
                                                </td>
                                                <td class="text-center">
                                                    <button class="btn btn-sm ff_more" title="Open"
                                                            t-on-click.stop="() => this.openEmployee(person)">
                                                        <i class="fa fa-ellipsis-h"/>
                                                    </button>
                                                </td>
                                            </tr>
                                            <tr t-if="!people.length">
                                                <td colspan="8" class="text-muted">Nobody matches this search.</td>
                                            </tr>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Row 4: counters with a seven-day line -->
                    <div class="row g-3 mt-0">
                        <t t-foreach="counterCards" t-as="card" t-key="card.key">
                            <div class="col-xl-3 col-md-6">
                                <div class="ff_card ff_counter h-100" t-attf-style="--tile: {{card.color}}"
                                     t-att-role="card.open ? 'button' : ''" t-on-click="() => card.open and card.open()">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa" t-att-class="card.icon"/></div>
                                        <div class="ff_card_title" t-esc="card.label"/>
                                    </div>
                                    <div class="d-flex align-items-end">
                                        <div>
                                            <div class="d-flex align-items-center gap-2">
                                                <span class="ff_big text-nowrap" t-esc="card.display"/>
                                                <span class="ff_trend"
                                                      t-att-class="card.change >= 0 ? 'ff_trend_up' : 'ff_trend_down'">
                                                    <i class="fa" t-att-class="card.change >= 0 ? 'fa-arrow-up' : 'fa-arrow-down'"/>
                                                    <t t-esc="Math.abs(card.change)"/>%
                                                </span>
                                            </div>
                                            <div class="mt-2">
                                                <span class="ff_chip"><t t-esc="card.yesterdayText"/> <t t-esc="card.compare"/></span>
                                            </div>
                                        </div>
                                        <svg t-if="card.series" class="ff_spark ms-auto" viewBox="0 0 100 40" preserveAspectRatio="none">
                                            <path t-att-d="spark(card.series, true)" t-attf-fill="{{card.color}}" fill-opacity="0.12"/>
                                            <path t-att-d="spark(card.series)" fill="none" t-attf-stroke="{{card.color}}"
                                                  stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                        <i t-else="" class="fa ff_counter_ghost ms-auto" t-att-class="card.icon"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>

                    <!-- Row 5: hours and visits -->
                    <div class="row g-3 mt-0">
                        <div class="col-xl-6">
                            <div class="ff_card h-100 ff_centered">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-clock-o"/></div>
                                    <div class="ff_card_title">Daily Average Working Hours</div>
                                    <span class="ff_period_chip ms-auto" t-esc="periodName"/>
                                </div>
                                <FfChart t-if="state.data.working_hours.length"
                                         type="'bar'"
                                         labels="chartLabels(state.data.working_hours)"
                                         datasets="[{label: 'Hours', data: chartValues(state.data.working_hours, 'hours'), color: '#1a56db'}]"
                                         height="220"/>
                                <div t-else="" class="text-muted small py-4">No attendance in this period yet.</div>
                            </div>
                        </div>

                        <div class="col-xl-6">
                            <div class="ff_card h-100 ff_centered">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-flag"/></div>
                                    <div class="ff_card_title">Visit Status Overview</div>
                                    <span class="ff_period_chip ms-auto" t-esc="periodName"/>
                                </div>
                                <div class="d-flex flex-wrap gap-3 mb-2 small">
                                    <div><span class="ff_dot" style="background:#16A34A"/><b t-esc="state.data.visits.done"/> Done</div>
                                    <div><span class="ff_dot" style="background:#1A56DB"/><b t-esc="state.data.visits.ongoing"/> Ongoing</div>
                                    <div><span class="ff_dot" style="background:#F59E0B"/><b t-esc="state.data.visits.planned"/> Planned</div>
                                    <div><span class="ff_dot" style="background:#DC2626"/><b t-esc="state.data.visits.missed"/> Missed</div>
                                </div>
                                <div class="ff_stack">
                                    <div class="ff_stack_part" t-attf-style="width: {{visitShare.done}}%; background:#16A34A"/>
                                    <div class="ff_stack_part" t-attf-style="width: {{visitShare.ongoing}}%; background:#1A56DB"/>
                                    <div class="ff_stack_part" t-attf-style="width: {{visitShare.planned}}%; background:#F59E0B"/>
                                    <div class="ff_stack_part" t-attf-style="width: {{visitShare.missed}}%; background:#DC2626"/>
                                </div>
                                <div class="small text-muted mt-2">
                                    <t t-esc="state.data.visits.total"/> visits in <t t-esc="state.data.period_label"/>
                                </div>
                                <div class="row g-2 mt-2">
                                    <t t-foreach="visitTiles" t-as="tile" t-key="tile.key">
                                        <div class="col-6 col-md-3">
                                            <div class="ff_vtile" t-attf-style="--tile: {{tile.color}}">
                                                <i class="fa" t-att-class="tile.icon"/>
                                                <div>
                                                    <div class="ff_vtile_value"><t t-esc="tile.pct"/>%</div>
                                                    <div class="ff_vtile_label" t-esc="tile.label"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                                <button class="btn btn-link btn-sm p-0 mt-2"
                                        t-on-click="() => this.openModel('ff.visit', 'Visits')">
                                    Open visits <i class="fa fa-arrow-right ms-1"/>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Row 6: money -->
                    <div class="row g-3 mt-0 pb-3">
                        <div class="col-xl-4" t-if="state.data.expenses">
                            <div class="ff_card h-100 ff_centered">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-credit-card"/></div>
                                    <div class="ff_card_title">Expense Overview</div>
                                    <span class="ff_period_chip ms-auto" t-esc="periodName"/>
                                </div>
                                <div class="d-flex align-items-center gap-3">
                                    <div class="ff_ring" style="width: 150px; flex: none;">
                                        <div class="ff_ring_center">
                                            <div class="ff_ring_label">Total</div>
                                            <div class="ff_ring_value" t-esc="money(state.data.expenses.total)"/>
                                        </div>
                                        <FfChart type="'doughnut'" legend="false"
                                                 labels="['Approved', 'Pending', 'Draft', 'Rejected']"
                                                 datasets="[{data: [
                                                    state.data.expenses.approved.amount,
                                                    state.data.expenses.submitted.amount,
                                                    state.data.expenses.draft.amount,
                                                    state.data.expenses.rejected.amount],
                                                    colors: ['#16a34a', '#f59e0b', '#1a56db', '#dc2626']}]"
                                                 money="true" currency="state.data.currency"
                                                 height="150"/>
                                    </div>
                                    <div class="flex-grow-1 small">
                                        <div class="text-muted mb-1"><t t-esc="state.data.expenses.count"/> claims</div>
                                        <t t-foreach="expenseRows" t-as="line" t-key="line.label">
                                            <div class="d-flex align-items-center py-1">
                                                <span class="ff_dot" t-attf-style="background: {{line.color}}"/>
                                                <span t-esc="line.label"/>
                                                <span class="ms-auto text-muted me-2"><t t-esc="line.count"/></span>
                                                <b class="ff_amount" t-esc="money(line.amount)"/>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                                <button class="btn btn-link btn-sm p-0 mt-2"
                                        t-on-click="() => this.openModel('ff.expense.claim', 'Expense Claims')">
                                    View claims <i class="fa fa-arrow-right ms-1"/>
                                </button>
                            </div>
                        </div>

                        <div class="col-xl-4" t-if="state.data.collections">
                            <div class="ff_card h-100 ff_centered">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-database"/></div>
                                    <div class="ff_card_title">Collections</div>
                                    <span class="ff_period_chip ms-auto" t-esc="periodName"/>
                                </div>
                                <div class="ff_big" t-esc="money(state.data.collections.total)"/>
                                <div class="small text-muted mb-3">
                                    <t t-esc="state.data.collections.count"/> collections in
                                    <t t-esc="state.data.period_label"/>
                                </div>
                                <t t-foreach="collectionRows" t-as="line" t-key="line.label">
                                    <div class="ff_coll_row" t-attf-style="--tile: {{line.color}}">
                                        <span class="ff_dot" t-attf-style="background: {{line.color}}"/>
                                        <span t-esc="line.label"/>
                                        <span class="ms-auto text-muted me-3 small"><t t-esc="line.count"/></span>
                                        <b class="ff_amount" t-esc="money(line.amount)"/>
                                    </div>
                                </t>
                                <button class="btn btn-link btn-sm p-0 mt-3"
                                        t-on-click="() => this.openModel('ff.collection', 'Collections')">
                                    View collections <i class="fa fa-arrow-right ms-1"/>
                                </button>
                            </div>
                        </div>

                        <div class="col-xl-4" t-if="state.data.orders">
                            <div class="ff_card h-100 ff_centered">
                                <div class="ff_card_head">
                                    <div class="ff_card_icon"><i class="fa fa-trophy"/></div>
                                    <div class="ff_card_title">Top 5 Sellers</div>
                                    <span class="ff_period_chip ms-auto" t-esc="periodName"/>
                                </div>
                                <div class="ff_big" t-esc="money(state.data.orders.total)"/>
                                <div class="small text-muted mb-2">
                                    <t t-esc="state.data.orders.count"/> <t t-esc="state.data.orders.word or 'orders'"/> in
                                    <t t-esc="state.data.period_label"/>
                                </div>
                                <div t-foreach="state.data.orders.top" t-as="row" t-key="row.id"
                                     class="ff_seller_row d-flex align-items-center">
                                    <span class="ff_rank" t-esc="row_index + 1"/>
                                    <span class="text-truncate" t-esc="row.name"/>
                                    <span class="ms-auto small text-muted me-3">
                                        <t t-esc="row.count"/> <t t-esc="state.data.orders.word or 'orders'"/>
                                    </span>
                                    <span class="fw-bold" t-esc="money(row.amount)"/>
                                </div>
                                <div t-if="!state.data.orders.top.length" class="text-muted small">
                                    Nothing in this period.
                                </div>
                                <div class="text-end">
                                    <button class="btn btn-link btn-sm p-0 mt-2" t-on-click="() => this.openSection('orders')">
                                        View all <i class="fa fa-arrow-right ms-1"/>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </t>
            </div>

            <!-- Live Location -->
            <div class="ff_live flex-grow-1 d-flex flex-column" t-if="state.section === 'live'">
                <div class="ff_live_bar d-flex align-items-center gap-2 flex-wrap">
                    <div class="ff_card_head mb-0">
                        <div class="ff_card_icon"><i class="fa fa-map-marker"/></div>
                        <div>
                            <div class="ff_card_title">Live Location</div>
                            <div class="ff_card_sub"><t t-esc="livePeople.length"/> of <t t-esc="liveCounts.all"/> employees</div>
                        </div>
                    </div>

                    <div class="ff_seg">
                        <button class="ff_seg_btn" t-att-class="state.liveView === 'live' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.openLiveView('live')">
                            <i class="fa fa-location-arrow me-1"/>Live
                        </button>
                        <button class="ff_seg_btn" t-att-class="state.liveView === 'timeline' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.openLiveView('timeline')">
                            <i class="fa fa-clock-o me-1"/>Timeline
                        </button>
                    </div>

                    <t t-if="state.liveView === 'live'">
                    <div class="ff_panel_search_wrap">
                        <i class="fa fa-search"/>
                        <input type="text" class="form-control form-control-sm ff_panel_search"
                               placeholder="Search employee, team or place" t-on-input="onLiveSearch"/>
                    </div>

                    <div class="ff_seg">
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'all' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('all')">All <t t-esc="liveCounts.all"/></button>
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'in' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('in')">Punched in <t t-esc="liveCounts.in"/></button>
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'off' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('off')">Punched out <t t-esc="liveCounts.off"/></button>
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'at_client' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('at_client')">At customer <t t-esc="liveCounts.at_client"/></button>
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'inactive' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('inactive')">Idle <t t-esc="liveCounts.inactive"/></button>
                        <button class="ff_seg_btn" t-att-class="state.liveFilter === 'no_signal' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setLiveFilter('no_signal')">No signal <t t-esc="liveCounts.no_signal"/></button>
                    </div>

                    <select class="form-select form-select-sm ff_mini_select" t-on-change="setBattery">
                        <option value="all">Battery: all</option>
                        <option value="low">Below 20%</option>
                        <option value="ok">20% and above</option>
                    </select>
                    <select class="form-select form-select-sm ff_mini_select" t-on-change="setGps">
                        <option value="all">GPS: all</option>
                        <option value="on">GPS on</option>
                        <option value="off">GPS off</option>
                    </select>
                    <div class="form-check form-switch mb-0">
                        <input class="form-check-input" type="checkbox" id="ff_live_clients"
                               t-att-checked="state.showClients" t-on-change="toggleClients"/>
                        <label class="form-check-label small" for="ff_live_clients">Show customers</label>
                    </div>
                    </t>

                    <t t-if="state.liveView === 'timeline'">
                        <select class="form-select form-select-sm ff_mini_select ff_employee_select"
                                t-on-change="onTimelineEmployee">
                            <option t-foreach="state.timelineEmployees" t-as="row" t-key="row.id"
                                    t-att-value="row.id" t-att-selected="row.id === state.timelineEmployeeId">
                                <t t-esc="row.name"/><t t-if="row.code"> (<t t-esc="row.code"/>)</t>
                            </option>
                        </select>
                        <button class="ff_icon_btn" title="Previous day" t-on-click="() => this.shiftDay(-1)">
                            <i class="fa fa-chevron-left"/>
                        </button>
                        <input type="date" class="form-control form-control-sm ff_mini_select"
                               t-att-value="state.timelineDate" t-on-change="onTimelineDate"/>
                        <button class="ff_icon_btn" title="Next day" t-on-click="() => this.shiftDay(1)">
                            <i class="fa fa-chevron-right"/>
                        </button>
                        <button class="ff_btn_primary" t-on-click="() => this.loadTimeline()">Apply</button>
                        <button class="ff_btn_primary" t-on-click="togglePlay"
                                t-att-disabled="!state.timeline or state.timeline.path.length &lt; 2">
                            <i class="fa" t-att-class="state.playing ? 'fa-pause' : 'fa-play'"/>
                            <span class="ms-1"><t t-esc="state.playing ? 'Pause' : 'Play'"/></span>
                        </button>
                    </t>

                    <div class="ms-auto d-flex align-items-center gap-2">
                        <small class="text-muted" t-if="state.updatedAt">Updated <t t-esc="state.updatedAt"/></small>
                        <button class="ff_icon_btn" title="Refresh" t-on-click="() => this.loadLive()">
                            <i class="fa fa-refresh"/>
                        </button>
                    </div>
                </div>

                <div class="d-flex flex-grow-1 overflow-hidden">
                    <div class="ff_live_list" t-if="state.liveView === 'live'">
                        <t t-if="!state.live">
                            <div class="ff_skeleton m-2" style="height: 160px;"/>
                            <div class="ff_skeleton m-2" style="height: 160px;"/>
                        </t>
                        <t t-elif="!livePeople.length">
                            <div class="p-3 text-muted small">Nobody matches these filters.</div>
                        </t>
                        <t t-foreach="livePeople" t-as="person" t-key="person.id">
                            <div class="ff_live_card"
                                 t-att-class="state.openId === person.id ? 'ff_live_card_open' : ''"
                                 t-attf-style="border-left-color: {{stateColor(person.state)}}"
                                 t-on-click="() => this.openPerson(person)">
                                <div class="d-flex align-items-center gap-2">
                                    <img class="ff_avatar" t-att-src="person.avatar" alt=""/>
                                    <div class="flex-grow-1 min-width-0">
                                        <div class="fw-bold text-truncate" t-esc="person.name"/>
                                        <div class="small text-muted text-truncate">
                                            <t t-if="person.code">(<t t-esc="person.code"/>)</t>
                                            <t t-if="person.phone"> · <t t-esc="person.phone"/></t>
                                        </div>
                                    </div>
                                    <div class="text-end small">
                                        <div t-if="person.battery"
                                             t-att-class="person.battery &lt; 20 ? 'text-danger fw-bold' : 'text-muted'">
                                            <i class="fa" t-att-class="person.is_charging ? 'fa-bolt' : 'fa-battery-half'"/>
                                            <t t-esc="person.battery"/>%
                                        </div>
                                        <i class="fa fa-map-marker" t-att-style="'color: ' + stateColor(person.state)"/>
                                    </div>
                                </div>

                                <div class="ff_live_state mt-2"
                                     t-attf-style="background: {{stateColor(person.state)}}14">
                                    <div class="d-flex align-items-center">
                                        <span class="small fw-bold" t-att-style="'color: ' + stateColor(person.state)">
                                            <t t-if="person.punched_in">
                                                Punched IN<t t-if="person.punched_in_at"> : <t t-esc="clock(person.punched_in_at)"/></t>
                                            </t>
                                            <t t-else="">Punched OUT</t>
                                        </span>
                                        <button class="btn btn-sm btn-primary ms-auto py-0"
                                                t-on-click.stop="() => this.openTimeline(person)">Timeline</button>
                                    </div>
                                    <div class="small text-muted mt-1">
                                        <i class="fa fa-map-marker me-1"/>
                                        <t t-if="person.at_client">At <t t-esc="person.at_client"/></t>
                                        <t t-elif="person.address"><t t-esc="person.address"/></t>
                                        <t t-elif="person.lat">
                                            <t t-esc="person.lat.toFixed(5)"/>, <t t-esc="person.lng.toFixed(5)"/>
                                        </t>
                                        <t t-else="">No location sent yet</t>
                                        <t t-if="person.last_ping_at"> · <t t-esc="ago(person.last_ping_at)"/></t>
                                    </div>
                                </div>

                                <!-- The day of the person, once their card is open -->
                                <div t-if="state.openId === person.id" class="ff_live_tabs mt-2" t-on-click.stop="() => {}">
                                    <div class="ff_tabs">
                                        <button class="ff_tab" t-att-class="state.tab === 'visits' ? 'ff_tab_active' : ''"
                                                t-on-click="() => this.setTab('visits')">
                                            Visits <t t-if="state.day">(<t t-esc="state.day.visits.length"/>)</t>
                                        </button>
                                        <button class="ff_tab" t-att-class="state.tab === 'orders' ? 'ff_tab_active' : ''"
                                                t-on-click="() => this.setTab('orders')">
                                            Orders <t t-if="state.day">(<t t-esc="state.day.orders.length"/>)</t>
                                        </button>
                                        <button class="ff_tab" t-att-class="state.tab === 'forms' ? 'ff_tab_active' : ''"
                                                t-on-click="() => this.setTab('forms')">
                                            Forms <t t-if="state.day">(<t t-esc="state.day.forms.length"/>)</t>
                                        </button>
                                    </div>

                                    <div t-if="state.dayLoading" class="ff_skeleton mt-2" style="height: 60px;"/>
                                    <t t-elif="state.day">
                                        <table class="table table-sm ff_day_table mb-0 mt-1">
                                            <t t-if="state.tab === 'visits'">
                                                <tr t-foreach="state.day.visits" t-as="visit" t-key="visit.id">
                                                    <td>
                                                        <span class="ff_dot"
                                                              t-attf-style="background: {{visit.state === 'done' ? '#16A34A' : '#1A56DB'}}"/>
                                                        <t t-esc="visit.client"/>
                                                        <div class="text-muted" t-if="visit.outcome" t-esc="visit.outcome"/>
                                                    </td>
                                                    <td class="text-end text-muted">
                                                        <t t-esc="clock(visit.check_in_at)"/>
                                                        <t t-if="visit.check_out_at">–<t t-esc="clock(visit.check_out_at)"/></t>
                                                        <div t-if="visit.duration_min"><t t-esc="visit.duration_min"/> min</div>
                                                    </td>
                                                </tr>
                                                <tr t-if="!state.day.visits.length">
                                                    <td class="text-muted">No visits today.</td>
                                                </tr>
                                            </t>
                                            <t t-if="state.tab === 'orders'">
                                                <tr t-foreach="state.day.orders" t-as="order" t-key="order.id">
                                                    <td>
                                                        <t t-esc="order.name"/>
                                                        <div class="text-muted" t-esc="order.client"/>
                                                    </td>
                                                    <td class="text-end">
                                                        <span class="fw-bold" t-esc="money(order.amount)"/>
                                                        <div class="text-muted"><t t-esc="clock(order.at)"/></div>
                                                    </td>
                                                </tr>
                                                <tr t-if="!state.day.orders.length">
                                                    <td class="text-muted">No orders today.</td>
                                                </tr>
                                            </t>
                                            <t t-if="state.tab === 'forms'">
                                                <tr t-foreach="state.day.forms" t-as="form" t-key="form.id">
                                                    <td>
                                                        <t t-esc="form.name"/>
                                                        <div class="text-muted" t-esc="form.client"/>
                                                    </td>
                                                    <td class="text-end text-muted"><t t-esc="clock(form.at)"/></td>
                                                </tr>
                                                <tr t-if="!state.day.forms.length">
                                                    <td class="text-muted">No forms today.</td>
                                                </tr>
                                            </t>
                                        </table>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>


                    <!-- The day, told in order -->
                    <div class="ff_live_list ff_timeline_list" t-if="state.liveView === 'timeline'">
                        <t t-if="state.timelineLoading">
                            <div class="ff_skeleton m-2" style="height: 90px;"/>
                            <div class="ff_skeleton m-2" style="height: 200px;"/>
                        </t>
                        <t t-elif="state.timeline">
                            <div class="ff_tl_summary">
                                <div>
                                    <div class="ff_tl_sum_label">Punch in</div>
                                    <div class="ff_tl_sum_value" t-esc="clock(state.timeline.summary.punch_in) || '-'"/>
                                </div>
                                <div>
                                    <div class="ff_tl_sum_label">Punch out</div>
                                    <div class="ff_tl_sum_value" t-esc="clock(state.timeline.summary.punch_out) || '-'"/>
                                </div>
                                <div>
                                    <div class="ff_tl_sum_label">Hours</div>
                                    <div class="ff_tl_sum_value"><t t-esc="state.timeline.summary.hours"/></div>
                                </div>
                                <div>
                                    <div class="ff_tl_sum_label">Distance</div>
                                    <div class="ff_tl_sum_value"><t t-esc="state.timeline.summary.distance_km"/> km</div>
                                </div>
                                <div>
                                    <div class="ff_tl_sum_label">Visits</div>
                                    <div class="ff_tl_sum_value"><t t-esc="state.timeline.summary.visits"/></div>
                                </div>
                            </div>

                            <div class="ff_tl">
                                <t t-set="stop" t-value="0"/>
                                <t t-foreach="state.timeline.events" t-as="event" t-key="event_index">
                                    <t t-if="event.kind === 'travel'">
                                        <div class="ff_tl_travel">
                                            <i class="fa fa-motorcycle me-2"/>
                                            Travel: <span class="fw-bold"><t t-esc="event.distance_km"/> km</span>
                                            <span class="text-muted ms-2">(<t t-esc="event.minutes"/> min)</span>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <div class="ff_tl_row">
                                            <div class="ff_tl_time">
                                                <div class="fw-bold" t-esc="clock(event.at)"/>
                                                <div class="text-muted small" t-if="event.duration_min">
                                                    <t t-esc="event.duration_min"/> min
                                                </div>
                                            </div>
                                            <div class="ff_tl_mark">
                                                <span class="ff_tl_bullet"
                                                      t-attf-style="background: {{eventColor(event.kind)}}">
                                                    <i class="fa" t-att-class="eventIcon(event.kind)"/>
                                                </span>
                                            </div>
                                            <div class="ff_tl_body">
                                                <div class="fw-bold" t-esc="event.title"/>
                                                <div class="small text-muted" t-if="event.outcome" t-esc="event.outcome"/>
                                                <div class="small text-muted" t-if="event.note" t-esc="event.note"/>
                                                <div class="small fw-bold" t-if="event.amount" t-esc="money(event.amount)"/>
                                                <div class="small text-danger" t-if="event.inside_geofence === false">
                                                    Outside the customer geofence
                                                </div>
                                                <div class="ff_tl_photos" t-if="event.photos.length">
                                                    <img t-foreach="event.photos" t-as="photo" t-key="photo"
                                                         t-att-src="photo" alt=""/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </t>
                                <div t-if="!state.timeline.events.length" class="p-3 text-muted small">
                                    Nothing recorded on this day.
                                </div>
                            </div>
                        </t>
                    </div>

                    <div class="flex-grow-1 position-relative">
                        <div class="ff_live_canvas" t-ref="liveMap"/>
                        <div class="ff_tl_player" t-if="state.liveView === 'timeline' and state.timeline and state.timeline.path.length > 1">
                            <button class="ff_icon_btn" t-on-click="togglePlay">
                                <i class="fa" t-att-class="state.playing ? 'fa-pause' : 'fa-play'"/>
                            </button>
                            <div class="ff_tl_track">
                                <div class="ff_tl_track_fill" t-attf-style="width: {{playPercent}}%"/>
                            </div>
                            <span class="small text-muted"><t t-esc="playClock"/></span>
                        </div>
                        <div class="ff_warn ff_live_warn" t-if="state.liveError">
                            <i class="fa fa-exclamation-triangle me-1"/><t t-esc="state.liveError"/>
                        </div>
                        <div class="ff_warn ff_live_warn" t-if="state.live and !state.live.google_maps_key">
                            <i class="fa fa-info-circle me-1"/>
                            No Google Maps key yet — the list works without one. Paste a key in
                            Field Force settings to see the map.
                        </div>
                        <div class="ff_live_legend">
                            <span class="me-2" t-foreach="legend" t-as="item" t-key="item.key">
                                <span class="ff_dot" t-attf-style="background: {{item.color}}"/>
                                <span t-esc="item.label"/>
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Employees: cards, or the organisation as a tree -->
            <div class="ff_panel_main flex-grow-1 overflow-auto" t-if="state.section === 'people'">
                <div class="ff_panel_head ff_section_head d-flex align-items-center gap-3 flex-wrap">
                    <div class="ff_section_icon"><i class="fa fa-users"/></div>
                    <div>
                        <h3 class="ff_section_title mb-0">Employees</h3>
                        <div class="ff_section_sub">
                            <t t-esc="peopleStats.total"/> people ·
                            <t t-esc="peopleStats.managers"/> managers ·
                            <t t-esc="peopleStats.teams"/> teams
                        </div>
                    </div>
                    <div class="ff_seg ms-auto">
                        <button class="ff_seg_btn" t-att-class="state.peopleView === 'cards' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setPeopleView('cards')">
                            <i class="fa fa-th-large me-1"/>Card view
                        </button>
                        <button class="ff_seg_btn" t-att-class="state.peopleView === 'org' ? 'ff_seg_btn_active' : ''"
                                t-on-click="() => this.setPeopleView('org')">
                            <i class="fa fa-sitemap me-1"/>Hierarchy
                        </button>
                    </div>
                    <t t-if="state.peopleView === 'org'">
                        <button class="ff_icon_btn" title="Expand all" t-on-click="expandAll"><i class="fa fa-plus-square-o"/></button>
                        <button class="ff_icon_btn" title="Collapse all" t-on-click="collapseAll"><i class="fa fa-minus-square-o"/></button>
                    </t>
                    <button class="ff_outline_btn" t-on-click="() => this.openModel('hr.employee', 'Employees')">
                        <i class="fa fa-external-link me-1"/>Open in Odoo
                    </button>
                </div>

                <div class="ff_filter_bar">
                    <div class="ff_search_box">
                        <i class="fa fa-search"/>
                        <input type="text" class="form-control form-control-sm" placeholder="Search employee, team or position..."
                               t-att-value="state.peopleView === 'cards' ? state.liveSearch : state.orgSearch"
                               t-on-input="(ev) => state.peopleView === 'cards' ? this.onLiveSearch(ev) : this.onOrgSearch(ev)"/>
                    </div>
                    <select class="form-select form-select-sm ff_mini_select" t-on-change="(ev) => this.state.peopleDept = ev.target.value">
                        <option value="">All departments</option>
                        <option t-foreach="peopleChoices.departments" t-as="name" t-key="name" t-att-value="name"
                                t-att-selected="state.peopleDept === name" t-esc="name"/>
                    </select>
                    <select class="form-select form-select-sm ff_mini_select" t-on-change="(ev) => this.state.peopleTeam = ev.target.value">
                        <option value="">All teams</option>
                        <option t-foreach="peopleChoices.teams" t-as="name" t-key="name" t-att-value="name"
                                t-att-selected="state.peopleTeam === name" t-esc="name"/>
                    </select>
                    <select class="form-select form-select-sm ff_mini_select" t-on-change="(ev) => this.state.peopleStatus = ev.target.value">
                        <option value="" t-att-selected="!state.peopleStatus">All status</option>
                        <option value="in" t-att-selected="state.peopleStatus === 'in'">Punched in</option>
                        <option value="out" t-att-selected="state.peopleStatus === 'out'">Punched out</option>
                        <option value="field" t-att-selected="state.peopleStatus === 'field'" t-if="state.peopleView === 'cards'">At a customer</option>
                    </select>
                    <button class="ff_chip_btn" t-on-click="resetPeopleFilters"><i class="fa fa-refresh me-1"/>Reset</button>
                    <t t-if="state.peopleView === 'cards'">
                        <span class="ff_filter_label ms-auto">Sort by</span>
                        <select class="form-select form-select-sm ff_mini_select" t-on-change="(ev) => this.state.peopleSort = ev.target.value">
                            <option value="az" t-att-selected="state.peopleSort === 'az'">Name (A - Z)</option>
                            <option value="za" t-att-selected="state.peopleSort === 'za'">Name (Z - A)</option>
                            <option value="in" t-att-selected="state.peopleSort === 'in'">Punched in first</option>
                        </select>
                    </t>
                </div>

                <!-- Card view -->
                <t t-if="state.peopleView === 'cards'">
                    <div class="ff_people_stats">
                        <t t-foreach="peopleTiles" t-as="tile" t-key="tile.key">
                            <div class="ff_card ff_people_stat" t-attf-style="--tile: {{tile.color}}" role="button"
                                 t-on-click="() => this.state.peopleStatus = tile.status">
                                <div class="ff_people_stat_icon"><i class="fa" t-att-class="tile.icon"/></div>
                                <div>
                                    <div class="ff_att_kpi_label" t-esc="tile.label"/>
                                    <div class="d-flex align-items-center gap-2">
                                        <span class="ff_att_kpi_value" t-esc="tile.value"/>
                                        <span class="ff_kpi_chip m-0" t-if="tile.pct !== null"><t t-esc="tile.pct"/>%</span>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>

                    <div class="ff_people_grid">
                        <t t-if="!state.live">
                            <div class="ff_skeleton" style="height: 170px;"/>
                            <div class="ff_skeleton" style="height: 170px;"/>
                            <div class="ff_skeleton" style="height: 170px;"/>
                        </t>
                        <t t-foreach="peopleCards" t-as="person" t-key="person.id">
                            <div class="ff_person" t-attf-style="--tone: {{personTone(person)}}"
                                 t-on-click="() => this.openEmployee(person)">
                                <div class="d-flex align-items-start gap-3">
                                    <img class="ff_person_avatar" t-att-src="person.avatar" alt=""/>
                                    <div class="flex-grow-1 min-width-0">
                                        <div class="fw-bold text-truncate fs-6" t-esc="person.name"/>
                                        <div class="small text-muted text-truncate" t-if="person.code" t-esc="person.code"/>
                                        <div class="small text-muted text-truncate" t-if="person.phone">
                                            <i class="fa fa-phone me-1"/><t t-esc="person.phone"/>
                                        </div>
                                    </div>
                                    <span class="ff_io_pill" t-att-class="person.punched_in ? 'ff_io_in' : 'ff_io_out'">
                                        <span class="ff_dot"/><t t-esc="person.punched_in ? 'IN' : 'OUT'"/>
                                    </span>
                                    <button class="btn btn-sm ff_more px-1" title="Open"
                                            t-on-click.stop="() => this.openEmployee(person)">
                                        <i class="fa fa-ellipsis-v"/>
                                    </button>
                                </div>
                                <div class="ff_person_state">
                                    <div class="flex-grow-1 min-width-0">
                                        <div class="small fw-bold ff_person_line">
                                            <i class="fa fa-clock-o me-2"/>
                                            <t t-if="person.punched_in">Punched IN<t t-if="person.punched_in_at"> <t t-esc="clock(person.punched_in_at)"/></t></t>
                                            <t t-else="">Punched OUT</t>
                                        </div>
                                        <div class="small text-muted text-truncate mt-1">
                                            <i class="fa fa-map-marker me-2 text-danger"/>
                                            <t t-if="person.at_client">At <t t-esc="person.at_client"/></t>
                                            <t t-elif="person.address"><t t-esc="person.address"/></t>
                                            <t t-elif="person.lat"><t t-esc="person.lat.toFixed(5)"/>, <t t-esc="person.lng.toFixed(5)"/></t>
                                            <t t-else="">No location sent yet</t>
                                        </div>
                                    </div>
                                    <button class="ff_btn_primary ff_btn_sm" t-on-click.stop="() => this.openTimeline(person)">Timeline</button>
                                </div>
                                <div class="ff_org_chips justify-content-start mt-2">
                                    <span class="ff_person_chip" t-if="person.team" t-esc="person.team"/>
                                    <span class="ff_person_chip ff_person_chip_soft" t-if="person.department" t-esc="person.department"/>
                                </div>
                            </div>
                        </t>
                    </div>
                    <div t-if="state.live and !peopleCards.length" class="ff_card mt-3">
                        <div class="ff_empty"><i class="fa fa-users"/><div class="fw-bold">Nobody matches these filters.</div></div>
                    </div>
                </t>

                <!-- Hierarchy -->
                <t t-else="">
                    <div class="d-flex justify-content-end mt-3" t-if="state.org">
                        <div class="ff_card ff_org_stats">
                            <div><span class="ff_org_stat_icon" style="--tile:#1a56db"><i class="fa fa-users"/></span>
                                <div><div class="small text-muted">Total</div><b t-esc="state.org.total"/></div></div>
                            <div><span class="ff_org_stat_icon" style="--tile:#7c5cfc"><i class="fa fa-shield"/></span>
                                <div><div class="small text-muted">Managers</div><b t-esc="state.org.managers"/></div></div>
                            <div><span class="ff_org_stat_icon" style="--tile:#16a34a"><i class="fa fa-sitemap"/></span>
                                <div><div class="small text-muted">Teams</div><b t-esc="state.org.teams"/></div></div>
                        </div>
                    </div>
                    <div class="ff_chart_wrap" t-if="state.org">
                        <div class="ff_chart">
                            <ul class="ff_tree">
                                <t t-foreach="state.org.roots" t-as="root" t-key="root.id">
                                    <t t-call="ff_dashboard.OrgNode">
                                        <t t-set="node" t-value="root"/>
                                        <t t-set="depth" t-value="0"/>
                                    </t>
                                </t>
                            </ul>
                            <div t-if="!state.org.roots.length" class="ff_card text-muted">
                                No employees to show yet.
                            </div>
                        </div>
                    </div>
                    <div t-else="" class="ff_skeleton mt-3" style="height: 320px;"/>
                </t>
            </div>

            <!-- Report sections: attendance, leaves, expenses, orders -->
            <div class="ff_panel_main flex-grow-1 overflow-auto"
                 t-if="['attendance', 'leaves', 'expenses', 'orders', 'visits', 'demands', 'collections', 'targets'].includes(state.section)">
                <div class="ff_panel_head ff_section_head d-flex align-items-center gap-3 flex-wrap">
                    <div class="ff_section_icon">
                        <i class="fa" t-att-class="sections.find((s) => s.key === state.section).icon"/>
                    </div>
                    <div>
                        <h3 class="ff_section_title mb-0" t-esc="sections.find((s) => s.key === state.section).label"/>
                        <div class="ff_section_sub" t-esc="sectionSubtitle"/>
                    </div>
                    <span class="ff_date_chip ms-auto" t-if="state.report and state.report.period_label">
                        <i class="fa fa-calendar me-2"/><t t-esc="state.report.period_label"/>
                    </span>
                    <button class="ff_outline_btn" title="Download this report as Excel"
                            t-att-class="state.report and state.report.period_label ? '' : 'ms-auto'"
                            t-att-disabled="state.exporting or !state.report" t-on-click="() => this.exportSection()">
                        <i class="fa me-1" t-att-class="state.exporting ? 'fa-spinner fa-spin' : 'fa-download'"/>Export
                    </button>
                    <button class="ff_icon_btn" title="Refresh" t-on-click="() => this.refreshSection()">
                        <i class="fa fa-refresh"/>
                    </button>
                </div>

                <div class="ff_filter_bar" t-if="state.options">
                    <span class="ff_filter_label">Period</span>
                    <select class="form-select form-select-sm ff_mini_select" t-on-change="choosePeriod">
                        <option t-foreach="periodChoices" t-as="choice" t-key="choice.key"
                                t-att-value="choice.key" t-att-selected="state.period === choice.key"
                                t-esc="choice.label"/>
                    </select>
                    <t t-if="state.period === 'custom'">
                        <input type="date" class="form-control form-control-sm ff_mini_select"
                               t-att-value="state.filters.date_from"
                               t-on-change="(ev) => this.setDate('date_from', ev)"/>
                        <span class="small text-muted">to</span>
                        <input type="date" class="form-control form-control-sm ff_mini_select"
                               t-att-value="state.filters.date_to"
                               t-on-change="(ev) => this.setDate('date_to', ev)"/>
                    </t>
                    <span class="ff_chip" t-if="state.report and state.report.period_label"
                          t-esc="state.report.period_label"/>

                    <span class="ff_filter_label ms-2">Employee</span>
                    <select class="form-select form-select-sm ff_mini_select"
                            t-on-change="(ev) => this.setFilter('employee_id', ev)">
                        <option value="">All employees</option>
                        <option t-foreach="state.options.employees" t-as="row" t-key="row.id"
                                t-att-value="row.id" t-att-selected="state.filters.employee_id === row.id">
                            <t t-esc="row.name"/><t t-if="row.code"> (<t t-esc="row.code"/>)</t>
                        </option>
                    </select>
                    <span class="ff_filter_label ms-2">Department</span>
                    <select class="form-select form-select-sm ff_mini_select"
                            t-on-change="(ev) => this.setFilter('department_id', ev)">
                        <option value="">All departments</option>
                        <option t-foreach="state.options.departments" t-as="row" t-key="row.id"
                                t-att-value="row.id" t-att-selected="state.filters.department_id === row.id"
                                t-esc="row.name"/>
                    </select>
                    <span class="ff_filter_label ms-2">Team</span>
                    <select class="form-select form-select-sm ff_mini_select"
                            t-on-change="(ev) => this.setFilter('team_id', ev)">
                        <option value="">All teams</option>
                        <option t-foreach="state.options.teams" t-as="row" t-key="row.id"
                                t-att-value="row.id" t-att-selected="state.filters.team_id === row.id"
                                t-esc="row.name"/>
                    </select>
                    <button class="ff_chip_btn" t-if="filterCount" t-on-click="clearFilters">
                        <i class="fa fa-times me-1"/>Clear <t t-esc="filterCount"/>
                    </button>
                    <button class="ff_btn_primary ms-auto" t-on-click="() => this.refreshSection()">Apply</button>
                </div>

                <t t-if="state.reportLoading">
                    <div class="ff_skeleton mt-3" style="height: 120px;"/>
                    <div class="ff_skeleton mt-3" style="height: 260px;"/>
                </t>
                <t t-elif="!state.report">
                    <div class="ff_card mt-3 text-muted">
                        This area is not installed yet, so there is nothing to report.
                    </div>
                </t>
                <t t-else="">

                    <!-- Visits, demands, collections: one generic layout -->
                    <t t-if="state.report.generic and !state.report.visit and !state.report.demand and !state.report.collection and !state.report.target">
                        <div class="d-flex align-items-center mt-3">
                            <button class="ff_chip_btn ms-auto" t-on-click="() => this.drillGeneric()">
                                <i class="fa fa-external-link me-1"/>Open in Odoo
                            </button>
                        </div>
                        <div class="row g-3 mt-0">
                            <div class="col-xl-3 col-6" t-foreach="state.report.tiles" t-as="tile" t-key="tile_index">
                                <div class="ff_card ff_tile ff_stat h-100" t-att-style="'--tile:' + tile.color" role="button"
                                     t-on-click="() => this.drillGeneric(tile.domain)">
                                    <div class="ff_card_title" t-esc="tile.label"/>
                                    <div class="ff_big" t-esc="tile.money ? money(tile.value) : tile.value"/>
                                    <span class="ff_chip" t-if="tile.sub" t-esc="tile.sub"/>
                                </div>
                            </div>
                        </div>
                        <div class="row g-3 mt-0">
                            <div t-foreach="state.report.charts" t-as="chart" t-key="chart_index"
                                 t-att-class="chart.type === 'line' ? 'col-12' : 'col-xl-6'">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa" t-att-class="chart.icon"/></div>
                                        <div class="ff_card_title" t-esc="chart.title"/>
                                    </div>
                                    <div t-if="!chart.values.length" class="text-muted small py-4 text-center">Nothing in this period.</div>
                                    <FfChart t-else="" type="chart.type" horizontal="chart.horizontal"
                                             labels="genericChart(chart).labels"
                                             datasets="genericChart(chart).datasets"
                                             money="chart.money" currency="state.report.currency" legend="false"
                                             height="chart.type === 'line' ? 240 : 220"/>
                                </div>
                            </div>
                        </div>
                        <div class="ff_card mt-3 mb-3">
                            <div class="ff_card_head">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Records</div>
                                <span class="ms-auto small text-muted" t-if="state.report.rows.length === 200">latest 200 · Excel has all</span>
                            </div>
                            <div>
                                <div class="ff_table_scroll"><table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th t-foreach="state.report.columns" t-as="column" t-key="column.key"
                                                t-att-class="column.end ? 'text-end' : ''" t-esc="column.label"/>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openGenericRow(row)">
                                            <td t-foreach="state.report.columns" t-as="column" t-key="column.key"
                                                t-att-class="(column.end ? 'text-end ' : '') + (column.bold ? 'fw-bold' : '')">
                                                <div t-if="column.progress" class="d-flex align-items-center gap-2" style="min-width: 150px;">
                                                    <div class="progress flex-grow-1" style="height: 8px;">
                                                        <div class="progress-bar" role="progressbar"
                                                             t-att-class="row[column.key] >= 100 ? 'bg-success' : row[column.key] >= 60 ? 'bg-primary' : 'bg-warning'"
                                                             t-att-style="'width:' + Math.min(row[column.key] || 0, 100) + '%'"/>
                                                    </div>
                                                    <span class="small fw-bold" t-esc="(row[column.key] || 0) + '%'"/>
                                                </div>
                                                <span t-elif="column.pill" class="ff_pill" t-att-class="stateBadge(row[column.key])"
                                                      t-esc="stateWord(row[column.key])"/>
                                                <t t-else="" t-esc="cell(column, row)"/>
                                            </td>
                                        </tr>
                                        <tr t-if="!state.report.rows.length">
                                            <td t-att-colspan="state.report.columns.length" class="text-muted">Nothing in this period.</td>
                                        </tr>
                                    </tbody>
                                </table></div>
                            <t t-call="ff_dashboard.Pager"/>
                            </div>
                        </div>
                    </t>

                    <!-- Targets -->
                    <t t-if="state.section === 'targets' and state.report.target">
                        <div class="d-flex align-items-center mt-2">
                            <div class="ff_warn" t-if="state.report.empty_hint" t-esc="state.report.empty_hint"/>
                            <button class="btn btn-link btn-sm ms-auto" t-on-click="() => this.drillGeneric()">
                                <i class="fa fa-external-link me-1"/>Open in Odoo
                            </button>
                        </div>
                        <div class="row g-3 mt-0">
                            <t t-foreach="targetKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}">
                                        <div class="d-flex align-items-start gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="flex-grow-1 min-width-0">
                                                <div class="ff_kpi_label" t-esc="kpi.label"/>
                                                <div class="ff_target_value text-nowrap" t-esc="kpi.value"/>
                                                <div class="ff_kpi_note m-0">of <t t-esc="kpi.goal"/> target</div>
                                            </div>
                                            <div class="text-center">
                                                <div class="ff_mini_ring" t-attf-style="--pct: {{Math.min(kpi.percent, 100)}}">
                                                    <span><t t-esc="kpi.percent"/>%</span>
                                                </div>
                                                <span class="ff_trend mt-2"
                                                      t-att-class="kpi.change > 0 ? 'ff_trend_up' : kpi.change &lt; 0 ? 'ff_trend_down' : 'ff_trend_flat'">
                                                    <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                                    <t t-esc="Math.abs(kpi.change)"/>%
                                                </span>
                                                <div class="ff_kpi_note m-0 small">vs last month</div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-user"/></div>
                                        <div class="ff_card_title">Achievement by employee</div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.target.month_label"/>
                                    </div>
                                    <t t-if="state.report.target.by_employee.length">
                                        <div t-foreach="state.report.target.by_employee.slice(0, 5)" t-as="row" t-key="row_index" class="ff_bar_row">
                                            <span class="text-muted" style="width: 16px;" t-esc="row_index + 1"/>
                                            <img t-if="row.id" class="ff_avatar ff_avatar_round ff_avatar_sm"
                                                 t-att-src="'/web/image/hr.employee/' + row.id + '/avatar_128'" alt=""/>
                                            <span class="ff_bar_name text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(3, Math.min(row.achievement, 100))}}%; background: linear-gradient(90deg, #1a56db, #60a5fa);"/>
                                            </div>
                                            <span class="small text-muted text-nowrap" t-if="row.goal"><t t-esc="row.done"/> / <t t-esc="row.goal"/></span>
                                            <b class="text-primary" style="min-width: 3rem; text-align: right;"><t t-esc="row.achievement"/>%</b>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-bullseye"/>
                                        <div class="fw-bold">No targets set</div>
                                        <div class="small text-muted">Add employee targets for this month.</div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-bar-chart"/></div>
                                        <div class="ff_card_title">Visits vs Target Trend</div>
                                        <div class="ff_chart_legend ms-auto">
                                            <span><span class="ff_dot" style="background:#1A56DB"/>Visits</span>
                                            <span><span class="ff_dot" style="background:#94A3B8"/>Target</span>
                                        </div>
                                    </div>
                                    <FfChart type="'bar'"
                                             labels="state.report.target.trend.map((row) => row.label)"
                                             datasets="[
                                                {label: 'Visits', data: state.report.target.trend.map((row) => row.visits), color: '#1a56db'},
                                                {label: 'Target', type: 'line', data: state.report.target.trend.map((row) => row.target), borderColor: '#94a3b8', backgroundColor: '#94a3b8', borderDash: [5, 5], pointRadius: 2, fill: false, color: '#94a3b8'}]"
                                             legend="false" height="240"/>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Records</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search department, employee..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Level</th><th>For</th><th>Visits</th><th>New Customers</th>
                                            <th>Sales</th><th>Collections</th><th style="min-width: 180px;">Achievement</th>
                                            <th class="text-end">Incentive</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openGenericRow(row)">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td t-esc="row.level"/>
                                            <td class="fw-bold" t-esc="row.employee"/>
                                            <td class="text-nowrap" t-esc="row.visits"/>
                                            <td class="text-nowrap" t-esc="row.customers"/>
                                            <td class="text-nowrap" t-esc="row.sales"/>
                                            <td class="text-nowrap" t-esc="row.collections"/>
                                            <td>
                                                <div class="d-flex align-items-center gap-2">
                                                    <div class="ff_progress flex-grow-1 mt-0">
                                                        <div t-attf-style="width: {{Math.max(2, Math.min(row.achievement || 0, 100))}}%; background: {{(row.achievement || 0) >= 100 ? '#16a34a' : (row.achievement || 0) >= 60 ? '#1a56db' : '#f59e0b'}};"/>
                                                    </div>
                                                    <span class="small fw-bold"><t t-esc="row.achievement || 0"/>%</span>
                                                </div>
                                            </td>
                                            <td class="text-end fw-bold" t-esc="row.incentive"/>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openGenericRow(row)">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="10">
                                                <div class="ff_empty">
                                                    <i class="fa fa-bullseye"/>
                                                    <div class="fw-bold">No targets for this month.</div>
                                                    <div class="small text-muted">Targets you set will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Collections -->
                    <t t-if="state.section === 'collections' and state.report.collection">
                        <div class="row g-3 mt-0">
                            <t t-foreach="collectionKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="d-flex align-items-end">
                                            <div class="min-width-0">
                                                <div class="ff_kpi_value text-nowrap" t-esc="kpi.value"/>
                                                <div class="ff_kpi_chip_row" t-if="kpi.value !== money(0)">
                                                    <span class="ff_trend"
                                                          t-att-class="kpi.change > 0 ? (kpi.bad ? 'ff_trend_down' : 'ff_trend_up') : kpi.change &lt; 0 ? (kpi.bad ? 'ff_trend_up' : 'ff_trend_down') : 'ff_trend_flat'">
                                                        <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                                        <t t-esc="Math.abs(kpi.change)"/>%
                                                    </span>
                                                </div>
                                                <div class="ff_kpi_note" t-att-class="kpi.bad and kpi.count ? 'text-warning' : ''" t-esc="kpi.note"/>
                                            </div>
                                            <i class="fa ff_kpi_ghost" t-att-class="kpi.ghost"/>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-credit-card"/></div>
                                        <div class="ff_card_title">Collections by mode</div>
                                    </div>
                                    <div class="d-flex align-items-center gap-4 flex-wrap">
                                        <div style="width: 200px; flex: none;" class="ff_ring">
                                            <t t-if="state.report.collection.collected">
                                                <div class="ff_ring_center">
                                                    <div class="ff_ring_value fs-5" t-esc="money(state.report.collection.collected)"/>
                                                    <div class="ff_ring_label">Total</div>
                                                </div>
                                                <FfChart type="'doughnut'" legend="false"
                                                         labels="collectionModes.map((row) => row.name)"
                                                         datasets="[{data: collectionModes.map((row) => row.amount), colors: collectionModes.map((row) => row.color)}]"
                                                         money="true" currency="state.report.currency" height="200"/>
                                            </t>
                                            <div t-else="" class="ff_empty py-2">
                                                <div class="ff_ring_empty"/>
                                                <div class="fw-bold">No data to display</div>
                                            </div>
                                        </div>
                                        <div class="flex-grow-1">
                                            <div class="ff_off_row" t-foreach="collectionModes" t-as="row" t-key="row.name">
                                                <span class="ff_dot" t-attf-style="background: {{row.color}}"/>
                                                <span class="text-truncate" t-esc="row.name"/>
                                                <b class="ms-auto ff_amount" t-esc="money(row.amount)"/>
                                                <span class="text-muted small ms-3" style="min-width: 2.6rem; text-align: right;"><t t-esc="row.pct"/>%</span>
                                            </div>
                                            <div t-if="!collectionModes.length" class="text-muted small">No collections in this period.</div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-line-chart"/></div>
                                        <div class="ff_card_title">Collections trend</div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <FfChart type="'line'"
                                             labels="state.report.collection.series.map((row) => row.label)"
                                             datasets="[{label: 'Collected', data: state.report.collection.series.map((row) => row.amount), color: '#1a56db'}]"
                                             money="true" currency="state.report.currency" legend="false" height="230"/>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Collection Records</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search employee, customer, reference..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillGeneric()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Date</th><th>Employee</th><th>Customer</th><th>Mode</th><th>Reference</th>
                                            <th class="text-end">Amount</th><th>Status</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openGenericRow(row)">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td class="text-nowrap" t-esc="row.date"/>
                                            <td class="fw-bold" t-esc="row.employee"/>
                                            <td t-esc="row.customer"/>
                                            <td t-esc="row.mode"/>
                                            <td class="text-muted" t-esc="row.reference or '—'"/>
                                            <td class="text-end fw-bold text-nowrap" t-esc="money(row.amount)"/>
                                            <td>
                                                <span class="ff_state_pill"
                                                      t-att-class="row.state === 'received' ? 'ff_state_in' : row.state === 'submitted' ? 'ff_state_sub' : 'ff_state_held'"
                                                      t-esc="row.state === 'collected' ? 'With employee' : stateWord(row.state)"/>
                                            </td>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openGenericRow(row)">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="9">
                                                <div class="ff_empty">
                                                    <i class="fa fa-money"/>
                                                    <div class="fw-bold">No collections in this period.</div>
                                                    <div class="small text-muted">Payments collected in the app will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Demands -->
                    <t t-if="state.section === 'demands' and state.report.demand">
                        <div class="row g-3 mt-0">
                            <t t-foreach="demandKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="d-flex align-items-end">
                                            <div class="min-width-0">
                                                <div class="ff_kpi_value text-nowrap" t-esc="kpi.value"/>
                                                <div class="ff_kpi_chip_row">
                                                    <span class="ff_trend"
                                                          t-att-class="kpi.change > 0 ? (kpi.bad ? 'ff_trend_down' : 'ff_trend_up') : kpi.change &lt; 0 ? (kpi.bad ? 'ff_trend_up' : 'ff_trend_down') : 'ff_trend_flat'">
                                                        <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                                        <t t-esc="Math.abs(kpi.change)"/>%
                                                    </span>
                                                </div>
                                                <div class="ff_kpi_note" t-esc="kpi.note"/>
                                            </div>
                                            <svg class="ff_spark ms-auto" viewBox="0 0 100 40" preserveAspectRatio="none">
                                                <path t-att-d="spark(kpi.series, true)" t-attf-fill="{{kpi.color}}" fill-opacity="0.12"/>
                                                <path t-att-d="spark(kpi.series)" fill="none" t-attf-stroke="{{kpi.color}}"
                                                      stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                            </svg>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="ff_card mt-3">
                            <div class="d-flex align-items-center gap-3 flex-wrap mb-2">
                                <div class="ff_card_icon"><i class="fa fa-line-chart"/></div>
                                <div>
                                    <div class="ff_card_title">Demand value per day</div>
                                    <div class="ff_card_sub">
                                        <t t-esc="state.orderMeasure === 'count' ? 'Number of demands each day' : 'Trend of demand value for the selected period'"/>
                                    </div>
                                </div>
                                <div class="ff_seg ms-auto">
                                    <button class="ff_seg_btn" t-att-class="state.orderMeasure !== 'count' ? 'ff_seg_btn_active' : ''"
                                            t-on-click="() => this.state.orderMeasure = 'amount'">Value</button>
                                    <button class="ff_seg_btn" t-att-class="state.orderMeasure === 'count' ? 'ff_seg_btn_active' : ''"
                                            t-on-click="() => this.state.orderMeasure = 'count'">Count</button>
                                </div>
                                <span class="ff_period_chip" t-esc="state.report.period_label"/>
                            </div>
                            <FfChart type="'line'"
                                     labels="state.report.demand.series.map((row) => row.label)"
                                     datasets="[{label: state.orderMeasure === 'count' ? 'Demands' : 'Value', data: state.report.demand.series.map((row) => state.orderMeasure === 'count' ? row.count : row.amount), color: '#1a56db'}]"
                                     money="state.orderMeasure !== 'count'" currency="state.report.currency" legend="false"
                                     height="260"/>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-truck"/></div>
                                        <div class="ff_card_title">By distributor</div>
                                        <select class="form-select form-select-sm ff_pager_size ms-auto"
                                                t-on-change="(ev) => this.state.topN = parseInt(ev.target.value, 10)">
                                            <option value="5" t-att-selected="state.topN === 5">Top 5</option>
                                            <option value="10" t-att-selected="state.topN === 10">Top 10</option>
                                        </select>
                                    </div>
                                    <t t-if="state.report.demand.by_distributor.length">
                                        <div t-foreach="state.report.demand.by_distributor.slice(0, state.topN)" t-as="row" t-key="row.id" class="ff_bar_row">
                                            <span class="text-muted" style="width: 18px;" t-esc="row_index + 1"/>
                                            <span class="ff_bar_name ff_bar_name_wide text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(4, Math.round(row.amount * 100 / (state.report.demand.by_distributor[0].amount || 1)))}}%; background: linear-gradient(90deg, #1a56db, #60a5fa);"/>
                                            </div>
                                            <b class="ff_amount" t-esc="money(row.amount)"/>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-truck"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No demands in this period.</div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-user"/></div>
                                        <div class="ff_card_title">By employee</div>
                                        <select class="form-select form-select-sm ff_pager_size ms-auto"
                                                t-on-change="(ev) => this.state.topN = parseInt(ev.target.value, 10)">
                                            <option value="5" t-att-selected="state.topN === 5">Top 5</option>
                                            <option value="10" t-att-selected="state.topN === 10">Top 10</option>
                                        </select>
                                    </div>
                                    <t t-if="state.report.demand.by_employee.length">
                                        <div t-foreach="state.report.demand.by_employee.slice(0, state.topN)" t-as="row" t-key="row.id" class="ff_bar_row">
                                            <span class="text-muted" style="width: 18px;" t-esc="row_index + 1"/>
                                            <span class="ff_bar_name ff_bar_name_wide text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(4, Math.round(row.amount * 100 / (state.report.demand.by_employee[0].amount || 1)))}}%; background: linear-gradient(90deg, #1a56db, #60a5fa);"/>
                                            </div>
                                            <b class="ff_amount" t-esc="money(row.amount)"/>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-user"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No demands in this period.</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Records</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search reference, employee, outlet..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillGeneric()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Number</th><th>Date</th><th>Employee</th><th>Outlet</th><th>Distributor</th>
                                            <th class="text-end">Value</th><th>Status</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openGenericRow(row)">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td class="fw-bold text-nowrap" t-esc="row.name"/>
                                            <td class="text-nowrap" t-esc="row.date"/>
                                            <td t-esc="row.employee"/>
                                            <td t-esc="row.customer"/>
                                            <td class="text-muted" t-esc="row.distributor or '—'"/>
                                            <td class="text-end fw-bold text-nowrap" t-esc="money(row.amount)"/>
                                            <td><span class="ff_pill" t-att-class="stateBadge(row.state)" t-esc="stateWord(row.state)"/></td>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openGenericRow(row)">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="9">
                                                <div class="ff_empty">
                                                    <i class="fa fa-shopping-basket"/>
                                                    <div class="fw-bold">No demands in this period.</div>
                                                    <div class="small text-muted">Outlet demands will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Visits -->
                    <t t-if="state.section === 'visits' and state.report.visit">
                        <div class="row g-3 mt-0">
                            <t t-foreach="visitKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="d-flex align-items-end">
                                            <div>
                                                <div class="ff_kpi_value" t-esc="kpi.value"/>
                                                <div class="ff_kpi_chip_row">
                                                    <span class="ff_trend"
                                                          t-att-class="kpi.change > 0 ? (kpi.bad ? 'ff_trend_down' : 'ff_trend_up') : kpi.change &lt; 0 ? (kpi.bad ? 'ff_trend_up' : 'ff_trend_down') : 'ff_trend_flat'">
                                                        <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                                        <t t-esc="Math.abs(kpi.change)"/>%
                                                    </span>
                                                </div>
                                                <div class="ff_kpi_note" t-esc="kpi.note"/>
                                            </div>
                                            <svg class="ff_spark ms-auto" viewBox="0 0 100 40" preserveAspectRatio="none">
                                                <path t-att-d="spark(kpi.series, true)" t-attf-fill="{{kpi.color}}" fill-opacity="0.12"/>
                                                <path t-att-d="spark(kpi.series)" fill="none" t-attf-stroke="{{kpi.color}}"
                                                      stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                            </svg>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="ff_card mt-3">
                            <div class="d-flex align-items-center gap-3 flex-wrap mb-2">
                                <div class="ff_card_icon"><i class="fa fa-bar-chart"/></div>
                                <div>
                                    <div class="ff_card_title">Visits per day</div>
                                    <div class="ff_card_sub">Number of customer visits each day</div>
                                </div>
                                <div class="ff_chart_legend ms-auto">
                                    <span><span class="ff_dot" style="background:#16A34A"/>Onsite</span>
                                    <span><span class="ff_dot" style="background:#1A56DB"/>Offsite</span>
                                    <span><span class="ff_dot" style="background:#7C5CFC"/>Productive</span>
                                </div>
                                <span class="ff_period_chip" t-esc="state.report.period_label"/>
                            </div>
                            <FfChart type="'line'"
                                     labels="state.report.visit.series.map((row) => row.label)"
                                     datasets="[
                                        {label: 'Onsite', data: state.report.visit.series.map((row) => row.onsite), color: '#16a34a'},
                                        {label: 'Offsite', data: state.report.visit.series.map((row) => row.offsite), color: '#1a56db'},
                                        {label: 'Productive', data: state.report.visit.series.map((row) => row.productive), color: '#7c5cfc'}]"
                                     legend="false" height="260"/>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-user"/></div>
                                        <div class="ff_card_title">Visits by employee</div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <t t-if="state.report.visit.by_employee.length">
                                        <div t-foreach="state.report.visit.by_employee.slice(0, 5)" t-as="row" t-key="row.id" class="ff_bar_row">
                                            <span class="text-muted" style="width: 16px;" t-esc="row_index + 1"/>
                                            <img class="ff_avatar ff_avatar_round ff_avatar_sm"
                                                 t-att-src="'/web/image/hr.employee/' + row.id + '/avatar_128'" alt=""/>
                                            <span class="ff_bar_name text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(4, Math.round(row.count * 100 / (state.report.visit.by_employee[0].count || 1)))}}%; background: linear-gradient(90deg, #1a56db, #60a5fa);"/>
                                            </div>
                                            <b style="min-width: 2rem; text-align: right;" t-esc="row.count"/>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-users"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No visits in this period.</div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-pie-chart"/></div>
                                        <div class="ff_card_title">Visit type distribution</div>
                                    </div>
                                    <div class="d-flex align-items-center gap-4 flex-wrap">
                                        <div class="ff_ring_big" t-attf-style="--pct: {{visitMix.onsitePct}}; background: conic-gradient(#16a34a calc({{visitMix.onsitePct}} * 1%), #1a56db 0);">
                                            <div class="ff_ring_big_inner">
                                                <div class="ff_ring_big_value" t-esc="state.report.visit.total"/>
                                                <div class="small text-muted">Total Visits</div>
                                            </div>
                                        </div>
                                        <div class="flex-grow-1">
                                            <div class="ff_off_row">
                                                <span class="ff_dot" style="background:#16A34A"/>Onsite
                                                <b class="ms-auto" t-esc="state.report.visit.onsite"/>
                                                <span class="ff_off_pct text-primary fw-bold"><t t-esc="visitMix.onsitePct"/>%</span>
                                            </div>
                                            <div class="ff_off_row">
                                                <span class="ff_dot" style="background:#1A56DB"/>Offsite
                                                <b class="ms-auto" t-esc="state.report.visit.offsite"/>
                                                <span class="ff_off_pct text-primary fw-bold"><t t-esc="100 - visitMix.onsitePct"/>%</span>
                                            </div>
                                            <div class="ff_info_box ff_info_good mt-3" t-if="state.report.visit.total">
                                                <i class="fa fa-bar-chart"/>
                                                <div>
                                                    <div class="fw-bold" t-esc="visitMix.headline"/>
                                                    <div class="small"><t t-esc="visitMix.onsitePct"/>% of your visits are onsite.</div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Visit Records</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search employee, customer..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillGeneric()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Date</th><th>Employee</th><th>Customer</th><th>In – Out</th>
                                            <th class="text-center">Duration</th><th>Type</th><th>Outcome</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openGenericRow(row)">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td class="text-nowrap" t-esc="row.date"/>
                                            <td class="fw-bold" t-esc="row.employee"/>
                                            <td t-esc="row.customer"/>
                                            <td class="text-nowrap" t-esc="row.time"/>
                                            <td class="text-center text-primary text-nowrap"><t t-esc="row.minutes or 0"/> min</td>
                                            <td>
                                                <span class="ff_state_pill" t-att-class="row.type === 'offsite' ? 'ff_state_out' : 'ff_state_in'"
                                                      t-esc="row.type === 'offsite' ? 'Offsite' : 'Onsite'"/>
                                            </td>
                                            <td t-esc="row.outcome or '—'"/>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openGenericRow(row)">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="9">
                                                <div class="ff_empty">
                                                    <i class="fa fa-map-marker"/>
                                                    <div class="fw-bold">No visits in this period.</div>
                                                    <div class="small text-muted">Customer check-ins will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Attendance -->
                    <t t-if="state.section === 'attendance' and state.report.series">
                        <div class="d-flex align-items-center gap-2 mt-3" t-if="state.attendanceView === 'calendar'">
                            <div class="ff_seg">
                                <button class="ff_seg_btn"
                                        t-att-class="state.attendanceView === 'summary' ? 'ff_seg_btn_active' : ''"
                                        t-on-click="() => this.setAttendanceView('summary')">
                                    <i class="fa fa-bar-chart me-1"/>Summary
                                </button>
                                <button class="ff_seg_btn"
                                        t-att-class="state.attendanceView === 'calendar' ? 'ff_seg_btn_active' : ''"
                                        t-on-click="() => this.setAttendanceView('calendar')">
                                    <i class="fa fa-calendar me-1"/>Calendar
                                </button>
                            </div>
                            <button class="ff_chip_btn ms-auto" t-on-click="() => this.drillAttendance()">
                                <i class="fa fa-external-link me-1"/>Open in Odoo
                            </button>
                        </div>

                        <t t-if="state.attendanceView === 'calendar'">
                            <div class="ff_card mt-3 mb-3" t-if="state.calendar">
                                <div class="d-flex align-items-center gap-2 mb-3">
                                    <div class="ff_card_head mb-0">
                                        <div class="ff_card_icon"><i class="fa fa-calendar"/></div>
                                        <div>
                                            <div class="ff_card_title" t-esc="state.calendar.label"/>
                                            <div class="ff_card_sub">
                                                <t t-esc="state.calendar.totals.present"/> of
                                                <t t-esc="state.calendar.totals.expected"/> expected ·
                                                <t t-esc="state.calendar.totals.late"/> late ·
                                                <t t-esc="state.calendar.totals.on_leave"/> on leave
                                            </div>
                                        </div>
                                    </div>
                                    <div class="ms-auto d-flex align-items-center gap-2">
                                        <button class="ff_chip_btn" t-on-click="() => this.shiftMonth(-1)">
                                            <i class="fa fa-chevron-left me-1"/>Previous month
                                        </button>
                                        <input type="month" class="form-control form-control-sm ff_mini_select"
                                               t-att-value="state.calendar.month.slice(0, 7)"
                                               t-on-change="pickMonth"/>
                                        <button class="ff_chip_btn" t-on-click="() => this.shiftMonth(1)"
                                                t-att-disabled="isCurrentMonth">
                                            Next month<i class="fa fa-chevron-right ms-1"/>
                                        </button>
                                        <button class="ff_chip_btn" t-if="!isCurrentMonth" t-on-click="thisMonth">
                                            <i class="fa fa-calendar-o me-1"/>This month
                                        </button>
                                    </div>
                                </div>

                                <div class="ff_cal_head">
                                    <span t-foreach="['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']"
                                          t-as="name" t-key="name" t-esc="name"/>
                                </div>
                                <div class="ff_cal">
                                    <t t-foreach="state.calendar.weeks" t-as="week" t-key="week_index">
                                        <t t-foreach="week" t-as="cell" t-key="cell_index">
                                            <div t-if="!cell" class="ff_cal_cell ff_cal_empty"/>
                                            <div t-else="" class="ff_cal_cell"
                                                 t-att-class="(cell.today ? 'ff_cal_today ' : '') + (state.openDay and state.openDay.date === cell.date ? 'ff_cal_open' : '')"
                                                 t-on-click="() => this.openDayDetail(cell)">
                                                <div class="ff_cal_top">
                                                    <span class="ff_cal_day" t-esc="cell.day"/>
                                                    <span class="ff_cal_count" t-if="!cell.future">
                                                        <t t-esc="cell.present"/>/<t t-esc="cell.boxes.length"/>
                                                    </span>
                                                </div>
                                                <div class="ff_cal_boxes">
                                                    <span t-foreach="visibleBoxes(cell)" t-as="box" t-key="box.id"
                                                          class="ff_cal_box" t-att-class="boxClass(box.status)"/>
                                                    <span class="ff_cal_more" t-if="hiddenBoxes(cell)">
                                                        +<t t-esc="hiddenBoxes(cell)"/>
                                                    </span>
                                                </div>

                                                <!-- The hover card: the day in words, and a way into it -->
                                                <div class="ff_cal_pop" t-if="!cell.future">
                                                    <div class="ff_cal_pop_head">
                                                        <span class="fw-bold">
                                                            <t t-esc="cell.weekday"/> <t t-esc="cell.date"/>
                                                        </span>
                                                        <span class="ff_chip ms-auto">
                                                            <t t-esc="cell.present"/>/<t t-esc="cell.boxes.length"/>
                                                        </span>
                                                    </div>
                                                    <div class="ff_cal_pop_counts">
                                                        <span><span class="ff_cal_box ff_box_present"/>
                                                            <t t-esc="cell.present - cell.late"/> present</span>
                                                        <span><span class="ff_cal_box ff_box_late"/>
                                                            <t t-esc="cell.late"/> late</span>
                                                        <span><span class="ff_cal_box ff_box_absent"/>
                                                            <t t-esc="cell.absent"/> absent</span>
                                                        <span><span class="ff_cal_box ff_box_leave"/>
                                                            <t t-esc="cell.leave"/> on leave</span>
                                                    </div>
                                                    <div class="ff_cal_pop_people">
                                                        <div t-foreach="popPeople(cell)" t-as="box" t-key="box.id"
                                                             class="ff_cal_pop_person">
                                                            <span class="ff_cal_box" t-att-class="boxClass(box.status)"/>
                                                            <span class="text-truncate" t-esc="box.name"/>
                                                            <span class="ms-auto text-muted">
                                                                <t t-if="box.at"><t t-esc="clock(box.at)"/></t>
                                                                <t t-else="" t-esc="box.status"/>
                                                            </span>
                                                        </div>
                                                        <div class="text-muted small" t-if="cell.boxes.length > 6">
                                                            and <t t-esc="cell.boxes.length - 6"/> more
                                                        </div>
                                                    </div>
                                                    <button class="ff_btn_primary w-100 mt-2"
                                                            t-on-click.stop="() => this.drillAttendance([['check_in', '&gt;=', cell.date + ' 00:00:00'], ['check_in', '&lt;=', cell.date + ' 23:59:59']])">
                                                        <i class="fa fa-external-link me-1"/>View attendance
                                                    </button>
                                                </div>
                                            </div>
                                        </t>
                                    </t>
                                </div>

                                <div class="ff_cal_legend">
                                    <span><span class="ff_cal_box ff_box_present"/> Present</span>
                                    <span><span class="ff_cal_box ff_box_late"/> Late</span>
                                    <span><span class="ff_cal_box ff_box_absent"/> Absent</span>
                                    <span><span class="ff_cal_box ff_box_leave"/> On leave</span>
                                    <span><span class="ff_cal_box ff_box_future"/> Not yet</span>
                                </div>

                                <div class="ff_day_detail mt-3" t-if="state.openDay">
                                    <div class="d-flex align-items-center mb-2">
                                        <span class="fw-bold">
                                            <t t-esc="state.openDay.weekday"/> <t t-esc="state.openDay.date"/>
                                        </span>
                                        <span class="ff_chip ms-2">
                                            <t t-esc="state.openDay.present"/> present ·
                                            <t t-esc="state.openDay.absent"/> absent
                                        </span>
                                        <button class="ff_chip_btn ms-auto"
                                                t-on-click="() => this.drillAttendance([['check_in', '&gt;=', state.openDay.date + ' 00:00:00'], ['check_in', '&lt;=', state.openDay.date + ' 23:59:59']])">
                                            <i class="fa fa-external-link me-1"/>Open that day
                                        </button>
                                    </div>
                                    <div class="ff_day_people">
                                        <div t-foreach="state.openDay.boxes" t-as="box" t-key="box.id"
                                             class="ff_day_person">
                                            <span class="ff_cal_box" t-att-class="boxClass(box.status)"/>
                                            <span class="text-truncate" t-esc="box.name"/>
                                            <span class="ms-auto small text-muted">
                                                <t t-if="box.at"><t t-esc="clock(box.at)"/> · <t t-esc="box.hours"/> h</t>
                                                <t t-else="" t-esc="box.status"/>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div t-else="" class="ff_skeleton mt-3" style="height: 320px;"/>
                        </t>

                        <t t-else="">
                        <!-- Five figures, each with its own line through the period -->
                        <div class="ff_att_kpis mt-3">
                            <t t-foreach="attendanceKpis" t-as="kpi" t-key="kpi.key">
                                <div class="ff_card ff_att_kpi" t-attf-style="--tile: {{kpi.color}}" role="button"
                                     t-on-click="kpi.open">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="ff_att_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                        <div class="min-width-0">
                                            <div class="ff_att_kpi_label" t-esc="kpi.label"/>
                                            <div class="ff_att_kpi_value" t-esc="kpi.value"/>
                                        </div>
                                    </div>
                                    <div class="d-flex align-items-end mt-2">
                                        <span t-if="kpi.change !== null" class="ff_trend"
                                              t-att-class="kpi.change > 0 ? (kpi.bad ? 'ff_trend_down' : 'ff_trend_up') : kpi.change &lt; 0 ? (kpi.bad ? 'ff_trend_up' : 'ff_trend_down') : 'ff_trend_flat'">
                                            <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                            <t t-esc="Math.abs(kpi.change)"/>%
                                        </span>
                                        <svg t-if="kpi.series" class="ff_spark ms-auto" viewBox="0 0 100 40" preserveAspectRatio="none">
                                            <path t-att-d="spark(kpi.series)" fill="none" t-attf-stroke="{{kpi.color}}"
                                                  stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                        <i t-else="" class="fa ff_counter_ghost ms-auto" t-att-class="kpi.icon"/>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="ff_card mt-3">
                            <div class="d-flex align-items-center gap-3 flex-wrap mb-2">
                                <div class="ff_card_icon"><i class="fa fa-bar-chart"/></div>
                                <div>
                                    <div class="ff_card_title">Attendance Overview</div>
                                    <div class="ff_card_sub">Present, late and absent count for each day</div>
                                </div>
                                <div class="ff_chart_legend mx-auto">
                                    <span><span class="ff_dot" style="background:#16A34A"/>Present</span>
                                    <span><span class="ff_dot" style="background:#F59E0B"/>Late</span>
                                    <span><span class="ff_dot" style="background:#DC2626"/>Absent</span>
                                </div>
                                <div class="ff_seg">
                                    <button class="ff_seg_btn ff_seg_btn_active">Summary</button>
                                    <button class="ff_seg_btn" t-on-click="() => this.setAttendanceView('calendar')">Calendar</button>
                                </div>
                            </div>
                            <FfChart type="'bar'"
                                     labels="chartLabels(state.report.series)"
                                     datasets="[
                                        {label: 'Present', data: chartValues(state.report.series, 'present'), color: '#16a34a'},
                                        {label: 'Late', data: chartValues(state.report.series, 'late'), color: '#f59e0b'},
                                        {label: 'Absent', data: chartValues(state.report.series, 'absent'), color: '#dc2626'}]"
                                     stacked="true" legend="false"
                                     height="260"/>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Employee Attendance</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search employee..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.exportSection()">
                                    <i class="fa fa-download me-1"/>Export
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Employee</th>
                                            <th class="text-center">Present</th>
                                            <th class="text-center">Absent</th>
                                            <th class="text-center">Late</th>
                                            <th class="text-center">Hours</th>
                                            <th class="text-center">Avg / Day</th>
                                            <th>Last Punch</th>
                                            <th class="text-center" style="width: 60px;"/>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openEmployee(row)">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td>
                                                <div class="d-flex align-items-center gap-2">
                                                    <img class="ff_avatar ff_avatar_round" t-att-src="row.avatar" alt=""/>
                                                    <div>
                                                        <div class="fw-bold" t-esc="row.name"/>
                                                        <div class="small text-muted" t-if="row.code" t-esc="row.code"/>
                                                    </div>
                                                </div>
                                            </td>
                                            <td class="text-center fw-bold text-success" t-esc="row.present"/>
                                            <td class="text-center fw-bold" t-att-class="row.absent ? 'text-danger' : 'text-muted'"
                                                t-esc="row.absent"/>
                                            <td class="text-center" t-att-class="row.late ? 'text-warning fw-bold' : 'text-muted'"
                                                t-esc="row.late"/>
                                            <td class="text-center fw-bold" t-esc="row.hours"/>
                                            <td class="text-center" t-esc="row.avg_hours"/>
                                            <td class="small text-muted" t-esc="row.last_in ? ago(row.last_in) : '-'"/>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openEmployee(row)">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="9" class="text-muted">Nobody matches this search.</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                        </t>
                    </t>

                    <!-- Leaves -->
                    <t t-if="state.section === 'leaves' and state.report.by_type">
                        <div class="row g-3 mt-0">
                            <t t-foreach="leaveKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="ff_kpi_value" t-esc="kpi.value"/>
                                        <div class="ff_kpi_note" t-esc="kpi.note"/>
                                        <i class="fa ff_kpi_ghost" t-att-class="kpi.ghost"/>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-7">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-bar-chart"/></div>
                                        <div class="ff_card_title">Requests by Leave Type</div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <t t-if="state.report.by_type.length">
                                        <FfChart type="'bar'"
                                                 labels="state.report.by_type.map((row) => row.name)"
                                                 datasets="[
                                                    {label: 'Approved', data: state.report.by_type.map((row) => row.approved || 0), color: '#16a34a'},
                                                    {label: 'Pending', data: state.report.by_type.map((row) => row.pending || 0), color: '#f59e0b'},
                                                    {label: 'Refused', data: state.report.by_type.map((row) => row.refused || 0), color: '#dc2626'}]"
                                                 legend="false" height="230"/>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-file-text-o"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No leave requests in this period.</div>
                                    </div>
                                    <div class="ff_chart_legend justify-content-center mt-2">
                                        <span><span class="ff_dot" style="background:#16A34A"/>Approved</span>
                                        <span><span class="ff_dot" style="background:#F59E0B"/>Pending</span>
                                        <span><span class="ff_dot" style="background:#DC2626"/>Refused</span>
                                    </div>
                                </div>
                            </div>

                            <div class="col-xl-5">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-plane"/></div>
                                        <div class="ff_card_title">Who is off today</div>
                                    </div>
                                    <div class="d-flex align-items-center gap-4 flex-wrap">
                                        <div class="ff_ring_big" t-attf-style="--pct: {{offToday.pct}}">
                                            <div class="ff_ring_big_inner">
                                                <div class="ff_ring_big_value" t-esc="offToday.off"/>
                                                <div class="small text-muted text-center">Employees<br/>on leave</div>
                                            </div>
                                        </div>
                                        <div class="flex-grow-1">
                                            <div class="ff_off_row">
                                                <span class="ff_dot" style="background:#16A34A"/>On Leave
                                                <b class="ms-auto" t-esc="offToday.off"/>
                                                <span class="ff_off_pct"><t t-esc="offToday.pct"/>%</span>
                                            </div>
                                            <div class="ff_off_row">
                                                <span class="ff_dot" style="background:#1A56DB"/>Working
                                                <b class="ms-auto" t-esc="offToday.working"/>
                                                <span class="ff_off_pct"><t t-esc="100 - offToday.pct"/>%</span>
                                            </div>
                                            <div class="ff_off_row">
                                                <span class="ff_dot" style="background:#CBD5E1"/>Total Employees
                                                <b class="ms-auto" t-esc="offToday.total"/>
                                                <span class="ff_off_pct"/>
                                            </div>
                                            <div t-if="!state.report.off_today.length" class="ff_info_box mt-3">
                                                <i class="fa fa-info-circle"/>
                                                <div>
                                                    <div class="fw-bold">Everybody is at work today!</div>
                                                    <div class="small">No team members are on leave.</div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div t-if="state.report.off_today.length" class="mt-3">
                                        <div t-foreach="state.report.off_today" t-as="row" t-key="row.id" class="ff_seller_row d-flex align-items-center">
                                            <span class="fw-bold" t-esc="row.name"/>
                                            <span class="ff_chip ms-2" t-esc="row.type"/>
                                            <span class="ms-auto small text-muted">until <t t-esc="row.until"/></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div class="ff_card_title">Leave Requests</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search employee..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillLeaves()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Employee</th><th>Type</th><th>From</th><th>To</th>
                                            <th class="text-center">Days</th><th>Status</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openModel('hr.leave', 'Time Off', [['id', '=', row.id]])">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td>
                                                <div class="d-flex align-items-center gap-2">
                                                    <img class="ff_avatar ff_avatar_round" t-att-src="row.avatar" alt=""/>
                                                    <span class="fw-bold" t-esc="row.employee"/>
                                                </div>
                                            </td>
                                            <td t-esc="row.type"/>
                                            <td t-esc="row.from"/>
                                            <td t-esc="row.to"/>
                                            <td class="text-center fw-bold" t-esc="row.days"/>
                                            <td><span class="ff_pill" t-att-class="stateBadge(row.state)"
                                                      t-esc="stateWord(row.state)"/></td>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openModel('hr.leave', 'Time Off', [['id', '=', row.id]])">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="8">
                                                <div class="ff_empty">
                                                    <i class="fa fa-file-text-o"/>
                                                    <div class="fw-bold">No requests in this period.</div>
                                                    <div class="small text-muted">There are no leave requests for the selected filters.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Expenses -->
                    <t t-if="state.section === 'expenses' and state.report.by_category">
                        <div class="row g-3 mt-0">
                            <t t-foreach="expenseKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="ff_kpi_value" t-esc="kpi.value"/>
                                        <div class="mt-1"><span class="ff_kpi_chip" t-if="kpi.chip" t-esc="kpi.chip"/></div>
                                        <div class="ff_kpi_note" t-if="kpi.note" t-esc="kpi.note"/>
                                        <i class="fa ff_kpi_ghost" t-att-class="kpi.ghost"/>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-tags"/></div>
                                        <div class="ff_card_title">Expenses by Category</div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <div class="d-flex align-items-center gap-4 flex-wrap">
                                        <div style="width: 190px; flex: none;" class="ff_ring">
                                            <t t-if="state.report.kpis.total">
                                                <div class="ff_ring_center">
                                                    <div class="ff_ring_label">Total</div>
                                                    <div class="ff_ring_value" t-esc="money(state.report.kpis.total)"/>
                                                </div>
                                                <FfChart type="'doughnut'" legend="false"
                                                         labels="expenseCategories.map((row) => row.name)"
                                                         datasets="[{data: expenseCategories.map((row) => row.amount), colors: expenseCategories.map((row) => row.color)}]"
                                                         money="true" currency="state.report.currency" height="190"/>
                                            </t>
                                            <div t-else="" class="ff_empty py-2">
                                                <div class="ff_ring_empty"/>
                                                <div class="fw-bold">No data to display</div>
                                                <div class="small text-muted">No expense claims in this period.</div>
                                            </div>
                                        </div>
                                        <div class="flex-grow-1">
                                            <div class="ff_off_row" t-foreach="expenseCategories" t-as="row" t-key="row.name">
                                                <span class="ff_dot" t-attf-style="background: {{row.color}}"/>
                                                <span class="text-truncate" t-esc="row.name"/>
                                                <span class="ms-auto text-muted small me-3"><t t-esc="row.pct"/>%</span>
                                                <b class="ff_amount" t-esc="money(row.amount)"/>
                                            </div>
                                            <div t-if="!expenseCategories.length" class="text-muted small">No categories yet.</div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-user"/></div>
                                        <div class="ff_card_title">Who Claimed Most</div>
                                        <select class="form-select form-select-sm ff_pager_size ms-auto" t-on-change="(ev) => this.state.topN = parseInt(ev.target.value, 10)">
                                            <option value="5" t-att-selected="state.topN === 5">Top 5</option>
                                            <option value="10" t-att-selected="state.topN === 10">Top 10</option>
                                        </select>
                                    </div>
                                    <t t-if="state.report.by_employee.length">
                                        <div t-foreach="state.report.by_employee.slice(0, state.topN)" t-as="row" t-key="row.id"
                                             class="ff_seller_row d-flex align-items-center gap-2">
                                            <span class="ff_rank" t-esc="row_index + 1"/>
                                            <span class="text-truncate fw-bold" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mx-2 mt-0">
                                                <div t-attf-style="width: {{Math.round(row.amount * 100 / (state.report.by_employee[0].amount || 1))}}%; background: linear-gradient(90deg, #7c5cfc, #a78bfa);"/>
                                            </div>
                                            <span class="small text-muted"><t t-esc="row.count"/> claims</span>
                                            <b class="ff_amount" t-esc="money(row.amount)"/>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-users"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No expense claims in this period.</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-file-text"/></div>
                                <div class="ff_card_title">Claims</div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search employee, category..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillExpenses()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Date</th><th>Employee</th><th>Category</th><th>Description</th>
                                            <th class="text-end">Amount</th><th>Status</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openModel('ff.expense.claim', 'Expense Claims', [['id', '=', row.id]])">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td class="text-nowrap" t-esc="row.date"/>
                                            <td>
                                                <div class="d-flex align-items-center gap-2">
                                                    <img t-if="row.avatar" class="ff_avatar ff_avatar_round" t-att-src="row.avatar" alt=""/>
                                                    <span class="fw-bold" t-esc="row.employee"/>
                                                </div>
                                            </td>
                                            <td t-esc="row.category"/>
                                            <td class="text-muted small ff_address">
                                                <t t-esc="row.note or row.partner or '—'"/>
                                            </td>
                                            <td class="text-end fw-bold text-nowrap" t-esc="money(row.amount)"/>
                                            <td><span class="ff_pill" t-att-class="stateBadge(row.state)"
                                                      t-esc="stateWord(row.state)"/></td>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openModel('ff.expense.claim', 'Expense Claims', [['id', '=', row.id]])">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td colspan="8">
                                                <div class="ff_empty">
                                                    <i class="fa fa-file-text-o"/>
                                                    <div class="fw-bold">No claims in this period.</div>
                                                    <div class="small text-muted">When employees raise expense claims, they will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>

                    <!-- Orders or demand -->
                    <t t-if="state.section === 'orders' and state.report.by_employee">
                        <div class="row g-3 mt-0">
                            <t t-foreach="orderKpis" t-as="kpi" t-key="kpi.key">
                                <div class="col-xl-3 col-md-6">
                                    <div class="ff_kpi h-100" t-attf-style="--tile: {{kpi.color}}" role="button"
                                         t-on-click="kpi.open">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="ff_kpi_icon"><i class="fa" t-att-class="kpi.icon"/></div>
                                            <div class="ff_kpi_label" t-esc="kpi.label"/>
                                        </div>
                                        <div class="d-flex align-items-end">
                                            <div>
                                                <div class="ff_kpi_value" t-esc="kpi.value"/>
                                                <div class="ff_kpi_chip_row">
                                                    <span class="ff_trend"
                                                          t-att-class="kpi.change > 0 ? 'ff_trend_up' : kpi.change &lt; 0 ? 'ff_trend_down' : 'ff_trend_flat'">
                                                        <i class="fa" t-att-class="kpi.change > 0 ? 'fa-arrow-up' : kpi.change &lt; 0 ? 'fa-arrow-down' : 'fa-minus'"/>
                                                        <t t-esc="Math.abs(kpi.change)"/>%
                                                    </span>
                                                </div>
                                                <div class="ff_kpi_note">vs last period</div>
                                            </div>
                                            <svg class="ff_spark ms-auto" viewBox="0 0 100 40" preserveAspectRatio="none">
                                                <path t-att-d="spark(kpi.series, true)" t-attf-fill="{{kpi.color}}" fill-opacity="0.12"/>
                                                <path t-att-d="spark(kpi.series)" fill="none" t-attf-stroke="{{kpi.color}}"
                                                      stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                            </svg>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>

                        <div class="ff_card mt-3">
                            <div class="d-flex align-items-center gap-3 flex-wrap mb-2">
                                <div class="ff_card_icon"><i class="fa fa-bar-chart"/></div>
                                <div>
                                    <div class="ff_card_title">
                                        <t t-esc="state.report.flow === 'demand' ? 'Demand Value Each Day' : 'Order Value Each Day'"/>
                                    </div>
                                    <div class="ff_card_sub">
                                        <t t-esc="state.orderMeasure === 'count' ? 'Number of documents each day' : 'Total value trend for the selected period'"/>
                                    </div>
                                </div>
                                <div class="ff_seg ms-auto">
                                    <button class="ff_seg_btn" t-att-class="state.orderMeasure !== 'count' ? 'ff_seg_btn_active' : ''"
                                            t-on-click="() => this.state.orderMeasure = 'amount'">Value</button>
                                    <button class="ff_seg_btn" t-att-class="state.orderMeasure === 'count' ? 'ff_seg_btn_active' : ''"
                                            t-on-click="() => this.state.orderMeasure = 'count'">Count</button>
                                </div>
                            </div>
                            <FfChart type="'line'"
                                     labels="chartLabels(state.report.series)"
                                     datasets="[{label: state.orderMeasure === 'count' ? 'Count' : 'Value', data: chartValues(state.report.series, state.orderMeasure === 'count' ? 'count' : 'amount'), color: '#1a56db'}]"
                                     money="state.orderMeasure !== 'count'" currency="state.report.currency" legend="false"
                                     height="260"/>
                        </div>

                        <div class="row g-3 mt-0">
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-trophy"/></div>
                                        <div>
                                            <div class="ff_card_title">Top Sellers</div>
                                            <div class="ff_card_sub">Top employees by value</div>
                                        </div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <t t-if="state.report.by_employee.length">
                                        <div t-foreach="state.report.by_employee.slice(0, 5)" t-as="row" t-key="row.id"
                                             class="ff_bar_row">
                                            <img class="ff_avatar ff_avatar_round ff_avatar_sm"
                                                 t-att-src="'/web/image/hr.employee/' + row.id + '/avatar_128'" alt=""/>
                                            <span class="ff_bar_name text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(4, Math.round(row.amount * 100 / (state.report.by_employee[0].amount || 1)))}}%; background: linear-gradient(90deg, #1a56db, #60a5fa);"/>
                                            </div>
                                            <b class="ff_amount"><t t-esc="money(row.amount)"/> <span class="text-muted fw-normal">(<t t-esc="row.count"/>)</span></b>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-users"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">Nothing sold in this period.</div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="ff_card h-100">
                                    <div class="ff_card_head">
                                        <div class="ff_card_icon"><i class="fa fa-cube"/></div>
                                        <div>
                                            <div class="ff_card_title">Top Products</div>
                                            <div class="ff_card_sub">Most ordered products</div>
                                        </div>
                                        <span class="ff_period_chip ms-auto" t-esc="state.report.period_label"/>
                                    </div>
                                    <t t-if="state.report.by_product.length">
                                        <div t-foreach="state.report.by_product.slice(0, 5)" t-as="row" t-key="row.id"
                                             class="ff_bar_row">
                                            <span class="ff_prod_icon" t-attf-style="--tile: {{['#7c5cfc', '#e11d48', '#f59e0b', '#16a34a', '#1a56db'][row_index % 5]}}">
                                                <i class="fa fa-cube"/>
                                            </span>
                                            <span class="ff_bar_name text-truncate" t-esc="row.name"/>
                                            <div class="ff_progress flex-grow-1 mt-0">
                                                <div t-attf-style="width: {{Math.max(4, Math.round(row.quantity * 100 / (state.report.by_product[0].quantity || 1)))}}%; background: {{['#7c5cfc', '#e11d48', '#f59e0b', '#16a34a', '#1a56db'][row_index % 5]}};"/>
                                            </div>
                                            <b class="ff_amount"><t t-esc="money(row.amount)"/> <span class="text-muted fw-normal">(<t t-esc="row.quantity"/>)</span></b>
                                        </div>
                                    </t>
                                    <div t-else="" class="ff_empty">
                                        <i class="fa fa-cube"/>
                                        <div class="fw-bold">No data to display</div>
                                        <div class="small text-muted">No products ordered in this period.</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="ff_card mt-3 mb-3">
                            <div class="d-flex align-items-center gap-2 flex-wrap mb-3">
                                <div class="ff_card_icon"><i class="fa fa-list"/></div>
                                <div>
                                    <div class="ff_card_title">
                                        <t t-esc="state.report.flow === 'demand' ? 'Outlet Demands' : 'Orders'"/>
                                    </div>
                                    <div class="ff_card_sub">
                                        <t t-esc="state.report.flow === 'demand' ? 'Outlet demands for this period' : 'Orders for this period'"/>
                                    </div>
                                </div>
                                <div class="ff_search_box ms-auto">
                                    <i class="fa fa-search"/>
                                    <input type="text" class="form-control form-control-sm" placeholder="Search outlet, employee..."
                                           t-att-value="state.rowSearch" t-on-input="searchRows"/>
                                </div>
                                <button class="ff_outline_btn" t-on-click="() => this.drillOrders()">
                                    <i class="fa fa-filter me-1"/>Open in Odoo
                                </button>
                            </div>
                            <div class="ff_table_scroll">
                                <table class="table ff_table align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th style="width: 44px;">#</th>
                                            <th>Reference</th><th>Date</th><th>Employee</th><th>Outlet</th>
                                            <th t-if="state.report.flow === 'demand'">Distributor</th>
                                            <th class="text-end">Amount</th><th>Status</th>
                                            <th class="text-center" style="width: 70px;">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="pagedRows" t-as="row" t-key="row.id" class="ff_row"
                                            t-on-click="() => this.openModel(state.report.flow === 'demand' ? 'ff.demand' : 'sale.order', 'Documents', [['id', '=', row.id]])">
                                            <td class="text-muted" t-esc="pageInfo.from + row_index"/>
                                            <td class="fw-bold text-nowrap" t-esc="row.name"/>
                                            <td class="text-nowrap small" t-esc="clock(row.date)"/>
                                            <td t-esc="row.employee"/>
                                            <td t-esc="row.partner"/>
                                            <td t-if="state.report.flow === 'demand'" class="text-muted" t-esc="row.distributor or '—'"/>
                                            <td class="text-end fw-bold text-nowrap" t-esc="money(row.amount)"/>
                                            <td><span class="ff_pill" t-att-class="stateBadge(row.state)"
                                                      t-esc="stateWord(row.state)"/></td>
                                            <td class="text-center">
                                                <button class="btn btn-sm ff_more" title="Open"
                                                        t-on-click.stop="() => this.openModel(state.report.flow === 'demand' ? 'ff.demand' : 'sale.order', 'Documents', [['id', '=', row.id]])">
                                                    <i class="fa fa-ellipsis-h"/>
                                                </button>
                                            </td>
                                        </tr>
                                        <tr t-if="!pagedRows.length">
                                            <td t-att-colspan="state.report.flow === 'demand' ? 9 : 8">
                                                <div class="ff_empty">
                                                    <i class="fa fa-file-text-o"/>
                                                    <div class="fw-bold">
                                                        <t t-esc="state.report.flow === 'demand' ? 'No outlet demands in this period.' : 'No orders in this period.'"/>
                                                    </div>
                                                    <div class="small text-muted">When they are created, they will appear here.</div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            <t t-call="ff_dashboard.Pager"/>
                        </div>
                    </t>
                </t>
            </div>
        </div>
    </t>

    <!-- Pages for a records table: range, size and previous / next -->
    <t t-name="ff_dashboard.Pager">
        <div class="ff_pager" t-if="state.report and state.report.rows.length">
            <span class="text-muted small">
                Showing <b t-esc="pageInfo.from"/>–<b t-esc="pageInfo.to"/> of <b t-esc="pageInfo.total"/>
            </span>
            <span class="ms-auto small text-muted">Rows</span>
            <select class="form-select form-select-sm ff_pager_size" t-on-change="setPageSize">
                <option t-foreach="[10, 25, 50, 100]" t-as="size" t-key="size" t-att-value="size"
                        t-att-selected="state.pageSize === size" t-esc="size"/>
            </select>
            <button class="ff_pager_btn" t-att-disabled="pageInfo.page &lt;= 1" t-on-click="() => this.setPage(1)"
                    title="First"><i class="fa fa-angle-double-left"/></button>
            <button class="ff_pager_btn" t-att-disabled="pageInfo.page &lt;= 1"
                    t-on-click="() => this.setPage(pageInfo.page - 1)" title="Previous"><i class="fa fa-angle-left"/></button>
            <span class="small fw-bold px-1"><t t-esc="pageInfo.page"/> / <t t-esc="pageInfo.pages"/></span>
            <button class="ff_pager_btn" t-att-disabled="pageInfo.page &gt;= pageInfo.pages"
                    t-on-click="() => this.setPage(pageInfo.page + 1)" title="Next"><i class="fa fa-angle-right"/></button>
            <button class="ff_pager_btn" t-att-disabled="pageInfo.page &gt;= pageInfo.pages"
                    t-on-click="() => this.setPage(pageInfo.pages)" title="Last"><i class="fa fa-angle-double-right"/></button>
        </div>
    </t>

    <!-- One person in the chart, with everybody under them on the branch below -->
    <t t-name="ff_dashboard.OrgNode">
        <li t-if="matchesOrg(node)" t-att-class="isCollapsed(node) ? 'ff_closed' : ''">
            <div class="ff_org_card"
                 t-attf-style="--depth-color: {{['#7c5cfc', '#16a34a', '#1a56db', '#f59e0b', '#e11d48'][depth % 5]}}"
                 t-on-click="() => this.openEmployee(node)">
                <button class="ff_org_menu" title="Open" t-on-click.stop="() => this.openEmployee(node)">
                    <i class="fa fa-ellipsis-v"/>
                </button>
                <div class="ff_org_top">
                    <img class="ff_org_avatar" t-att-src="node.avatar" alt=""/>
                    <span class="ff_org_dot" t-if="node.punched_in" title="Punched in"/>
                </div>
                <div class="ff_org_name text-truncate" t-esc="node.name"/>
                <div class="ff_org_job text-truncate">
                    <t t-if="node.job"><t t-esc="node.job"/></t>
                    <t t-else="">&#160;</t>
                </div>
                <div class="ff_org_chips">
                    <span class="ff_org_badge" t-if="node.team" t-esc="node.team"/>
                    <span class="ff_org_badge ff_org_badge_soft" t-if="node.department" t-esc="node.department"/>
                </div>
                <div class="ff_org_foot">
                    <div class="ff_org_fig">
                        <i class="fa fa-users"/>
                        <div>
                            <b t-esc="node.children.length or '–'"/>
                            <div>Team Members</div>
                        </div>
                    </div>
                    <div class="ff_org_fig">
                        <i class="fa fa-bullseye"/>
                        <div>
                            <b><t t-if="node.target !== null and node.target !== undefined"><t t-esc="node.target"/>%</t><t t-else="">–</t></b>
                            <div>Target Achieved</div>
                        </div>
                    </div>
                    <button class="ff_org_toggle" t-if="node.children.length" t-on-click.stop="() => this.toggleNode(node)">
                        <i class="fa" t-att-class="isCollapsed(node) ? 'fa-chevron-down' : 'fa-chevron-up'"/>
                    </button>
                </div>
            </div>

            <ul t-if="node.children.length and !isCollapsed(node)">
                <t t-foreach="node.children" t-as="child" t-key="child.id">
                    <t t-call="ff_dashboard.OrgNode">
                        <t t-set="node" t-value="child"/>
                        <t t-set="depth" t-value="depth + 1"/>
                    </t>
                </t>
            </ul>
        </li>
    </t>

</templates>
